# King's Valley 웹 포팅 계획

기준일: 2026-07-31

## 변경 기록 운영 규칙

- 코드, ROM 자산, 렌더링, 입력, 사운드 또는 검증 결과가 변경될 때마다 이 문서의 해당 체크리스트와 `진행 로그`를 같은 작업에서 갱신한다.
- 원본과 비교해 새 차이가 발견되면 `현재 문제와 원인` 또는 `잔여 작업`에 먼저 기록하고 구현한다.
- 구현했다고 원본 일치로 표시하지 않는다. 비교 검증을 통과한 항목만 완료로 표시한다.

## 목표

`src/kvalley.asm`과 `rom/RC-727.rom`을 기준으로 MSX King's Valley를 브라우저에서 원본의 화면 배치, 그래픽, 입력, 게임 진행, PSG 사운드에 최대한 가깝게 재현한다. 단순히 게임 규칙만 흉내 내는 임시 데모는 완료로 보지 않는다.

## 완료 기록

- [x] `tools/bin/sjasm.exe`로 ASM을 오류 없이 재어셈블한다.
- [x] 원본 ROM SHA-256을 기록하고 ROM 기반 자산 추출기를 만든다.
- [x] 15개 피라미드의 실제 맵 선택자, 반쪽 맵, 문, 모미아, 보석, 칼, 곡괭이, 회전문, 함정, 계단 데이터를 추출한다.
- [x] 실제 게임 패턴·색상·스프라이트 압축 데이터를 JavaScript에서 해제한다.
- [x] 원본 메뉴의 타일 배치(`King's`, `Valley`, 피라미드, 저작권 문구, 시작 문구)를 256×192 VRAM 좌표로 재현한다.
- [x] 게임 패턴의 실제 좌우 반전 타일, 6개 보석 패턴 반복 로딩, 패턴별 색상 테이블 주소를 복원한다.
- [x] 게임 HUD를 임의 픽셀 문자열에서 원본 문자 코드와 VRAM 위치(`SCORE`, `HI`, `REST`, `©KONAMI PYRAMID-xx`)로 교체한다.
- [x] 256x192 MSX 화면, 15개 스테이지 선택, 이동·점프·사다리·칼·곡괭이·보석·사망·클리어·엔딩의 기본 흐름을 연결한다.
- [x] 원본 `MusicIndex`의 시작/맵/게임/클리어/게임오버 스트림을 읽는 3채널 WebAudio 재생 경로를 연결한다.
- [x] `ref` 방식의 RGBA 프레임 버퍼, SCREEN 2 VRAM 미러, 32개 SAT와 1라인 4스프라이트 제한을 렌더러에 연결한다.
- [x] 원본 `flipSprites`의 16×16 좌우 블록 교환, 캐릭터 SAT Y 보정, 문 맵 ID 변환을 적용한다.
- [x] 자산 검사, JavaScript 구문 검사, 게임 상태 스모크 검사, 정적 HTTP 검사를 통과시킨다.

## 현재 문제와 원인

- [x] 타이틀 도입: Konami 로고 14단계 상승과 메뉴 로고 22열 도입을 원본 프레임 순서로 적용했다.
- [x] 화면: SCREEN 2 프레임 버퍼·실제 패턴 ID 이름 테이블·3개 패턴/색상 bank·SAT 32개·라인당 4개 제한·32열 검은 커튼·ROM 엔딩과 문 겹침 SAT를 통합했다.
- [x] 자산: 실제 패턴과 문 3프레임, 미라 등장/폭발, 보유 아이템 주인공, 곡괭이 파괴, 회전 칼 및 퇴장 문 `D8/DC/E0/E4` 4스프라이트를 연결했다.
- [x] 계단 비교: `getStairs3`의 행당 X 이동이 2타일이 아니라 1타일임을 수정해 stage 5의 방향·끝점·68개 계단 타일을 원본 배치와 대조했다.
- [x] 좌표 비교: `MapaRAMRoot`의 0행은 HUD 예약행으로 숨기고 1–21행을 화면 Y와 일치시킨 뒤 마지막 `0x14` 바닥행을 추가해 stage 1 플랫폼 접지 위치를 대조했다.
- [x] 입구 잔상: 입장 닫힘 프레임 뒤 입구 이름 테이블 타일을 `hidden` 상태로 전환해 중앙에 문 절반이 남지 않도록 했다.
- [x] 타이틀 시작 음악: 음표 길이를 원본 `tempo × (하위값 + 1)`로 수정해 `MUS_Start`를 156프레임에서 260프레임으로 복원했다.
- [x] 계단 접점: `COLOR_InGame`이 다음 심볼의 압축 바이트까지 이어지는 구조를 복원해 일반 방향 계단 끝단 `0x73/0x74`의 누락 색상을 적용했다.
- [x] 보석 광택: 중앙 보석 주위의 위·좌·우 패턴 `0x51/0x52/0x53`과 원본 8프레임 색상 순환을 적용했다.
- [x] 보석 구멍 충돌: 보석과 위·좌·우 광택이 원본처럼 기존 벽 맵을 `0x40–0x48` 계열로 치환해 stage 1 하단 구멍으로 진입할 수 있게 했다.
- [x] 보유 아이템: `GFX_ProtaKnife`와 `GFX_ProtaPico`를 주인공 상태에 따라 교체하고 좌우 반전 프레임까지 연결했다.
- [x] 곡괭이 기본 파괴: 원본 `tilesAnimCavar`의 `0x43 → 0x44 → 0` 단계를 적용하고 한 층 또는 연속 두 층의 굴착 상태를 구현했다.
- [x] 투척 칼 물리: 2px 비행 타일, 벽/회전문 충돌, 방향 반전, 8단계 포물선, 4프레임 단위 낙하, 바닥 칼 복귀와 재수집을 연결했다.
- [x] 투척 자세: 원본의 21프레임 주인공 action frame 8/9 전환과 16카운트 시점 실제 칼 방출을 적용했다.
- [x] 투척 칼 회전 프레임: 북/남 위상은 1타일, 대각선 위상만 2타일로 출력해 비행 중 옆 프레임 조각이 붙는 깨짐을 제거했다.
- [x] 적 피격 칼 회수: 미라에 맞은 칼을 삭제하지 않고 충돌·방향 반전·포물선·낙하를 거쳐 바닥의 수집 가능한 칼로 복귀시켰다.
- [x] 단일 액션 입력: 원본처럼 SPACE 하나가 무장 없을 때 점프, 칼 보유 시 투척, 곡괭이 보유 시 굴착을 선택하며 Z/Enter/Shift 액션 바인딩을 제거했다.
- [x] 바닥 아이템 맵: 칼의 `0x30/31/32` 배경 백업·복원과 곡괭이의 `0x80 → 0` 점유를 적용해 충돌·함정·굴착 판정이 그림과 일치한다.
- [x] 곡괭이 대상 판정: 발밑 앞쪽과 양쪽 벽 사이 옆 굴착을 분리하고 위 아이템·아래 회전문·방 경계 제한과 VERSION2 제거 ID `1`을 적용했다.
- [x] 주인공 계단 이동: `chkCogeEscalera`, `distPeldano`, `andaEscalera`, `chkFinEscalera`를 따라 상·하 진입, 좌·우 대각 이동, 2프레임 속도, 플랫폼 이탈을 적용했다.
- [x] 미라 계단 추적: 플레이어와 높이가 다르면 좌우 5타일의 계단을 탐색하고, 방향별 진입·타입별 속도·끝점 보행 복귀를 적용했다.
- [x] 미라 점프·낙하: 원본 점프 변위표, 플랫폼 끝 타입 분기, 벽 충돌 점프, 4픽셀 낙하와 착지를 별도 상태로 적용했다.
- [ ] 미라 생각·스트레스: 타입별 망설임, 보행 결정 주기, 충돌 누적·감쇠와 `0xAF` 자폭 전이를 적용했으며 동적 데모의 장기 경로·SAT 프레임을 계속 대조한다.
- [x] 회전문: 방향별 정적 맵 ID, 단방향 16프레임 밀기, 5단계 회전, 32스텝 통과와 방향 반전을 원본 상태기로 적용했다.
- [x] 함정 벽: 좌표를 초기 벽으로 오해한 배치를 제거하고, 천장 탐색·4픽셀 하강·물체 대기·플랫폼 종료·압사를 원본 상태기로 적용했다.
- [x] 엔딩: GOAL 대기 뒤 사막 피라미드 장면, 자동 좌측 보행, 일반 점프, 4배 점프, 축하 문구, 10,000점과 재시작 커튼을 원본 시간축으로 적용했다.
- [x] 메뉴 데모: 완성된 타이틀의 32프레임 무입력 대기, stage 5 오른쪽 입구, ROM의 2,000프레임 녹화 입력과 입력 시 메뉴 복귀를 적용했다.
- [x] GAME OVER: 사망 음악 종료 뒤 현재 플레이 화면의 12×5 타일 영역을 지우고 원본 글꼴·좌표·음악·184회 반속 대기를 적용했다.
- [x] 일시정지: 중앙 임의 박스를 제거하고 게임·지도 하단 우측 `PAUSING` 16프레임 점멸과 PSG 지속 재생을 적용했다.
- [x] 사운드: `setMusic` 스트림, AY 톤/노이즈, 16개 레지스터 프레임 추적, 결정론적 PCM/WAV와 파형 hash에 더해 openMSX 260프레임 PSG·WAV 직접 비교를 완료했다.
- [ ] 게임 로직: 기본 상태기는 구현했으며, 녹화 데모 0–1984프레임의 동적 SAT·고정소수점 이동 위상과 미라 장기 경로를 원본 RAM/VRAM 기준으로 계속 대조한다.

## 진행 순서

1. 실제 ROM의 인트로/메뉴 그래픽과 텍스트 VRAM 배치를 구현한다.
2. SCREEN 2의 16색 팔레트와 패턴·색상 테이블을 렌더러에 적용한다.
3. 원본 타일 ID 테이블과 스프라이트 주소/색상/플립 규칙을 게임 화면에 적용한다.
4. 원본 음악 인덱스와 PSG 음표 스트림을 WebAudio 3채널 재생기로 연결한다.
5. 크기·입력·일시정지·사운드 사용자 제스처를 브라우저에서 검증한다.
6. 원본 ASM 대비 남은 차이를 이 문서의 잔여 작업에 갱신한다.

## 잔여 작업

- [x] 실제 Konami 로고 상승 연출과 메뉴 로고의 프레임별 도입을 재현한다.
- [x] 실제 피라미드 지도/스크롤/입구·출구 연출을 재현한다. (ROM 지도 배경·이름 테이블, 88/224프레임 현재/목적지 화살표, 실제 목적지, 입구·출구, 32열 커튼과 방 사이 4타일×8프레임 스크롤 완료)
- [x] 스테이지 종료 후 현재 피라미드·출구 화살표에서 목적지 피라미드·입구 화살표로 이동하는 지도 시간축과 15→GOAL 분기를 재현한다.
- [x] 스테이지 입장 시 열린 문 뒤의 주인공, 8픽셀 자동 하강, 문 닫힘, 조작 시작 대기를 원본 프레임으로 재현한다.
- [x] 전체 타일 ID 변환과 파괴 애니메이션을 원본과 일치시킨다. (`0x43 → 0x44 → 0` 기본 파괴, 최대 두 층, 옆 벽 한/두 층, 아이템·회전문·방 경계 제한, 동적 맵 점유, `chkIncrust` 위치 보정과 action counter 프레임 단위 직역 완료)
- [x] 주인공의 양방향 계단 상·하 진입, 대각 이동, 끝점 접지와 이탈을 원본 판정으로 재현한다.
- [x] 회전문의 밀기 제한, 회전 애니메이션, 주인공 통과와 방향 반전을 원본 판정으로 재현한다.
- [x] 함정 벽의 발동 좌표, 천장 기점, 반 타일 하강, 물체 대기, 최종 정지와 압사를 재현한다.
- [x] 보석 완료 후 출구 레버, 3단계 개방, 자동 상승, 우측 문 가림, 3단계 폐쇄, 첫 클리어 보너스와 목적지 전환을 재현한다.
- [ ] 모미아 타입별 전체 판단, 계단 선택, 점프, 스트레스 자폭의 장기 데모 경로와 SAT 프레임까지 원본과 일치시킨다. (기본 상태기 완료, 같은 틱 판단 전환·논리 좌표·재등장 보행 카운터 보정 완료, 960프레임 이후 계단 선택 시점 잔여)
- [x] 15번 피라미드 GOAL 뒤 사막 엔딩, 자동 보행·점프, SPECIAL BONUS와 1번 피라미드 재시작을 원본 시간축으로 재현한다.
- [x] 메뉴 무입력 데모의 stage 5 진입, ROM 녹화 입력, 종료와 키 입력 복귀를 원본 시간축으로 재현한다.
- [x] 마지막 생명 사망 뒤 GAME OVER 오버레이, 음악, 대기와 타이틀 복귀를 원본 상태기로 재현한다.
- [x] 게임·지도 F1 일시정지와 하단 `PAUSING` 점멸을 원본 프레임·사운드 동작으로 재현한다.
- [x] 주인공의 고정소수점 보행, 상·하체 벽, 낮은 천장, 수평 점프 벽, 플랫폼 끝 낙하 좌표 보정을 원본 충돌 분기로 재현한다.
- [x] 타이틀·stage 1·지도·엔딩·GAME OVER의 256×192 전체 픽셀 framebuffer hash 회귀를 추가한다.
- [x] 퇴장 문 우측 절반을 SAT 슬롯 0–3의 4개 겹침 스프라이트로 그리고 주인공 슬롯 4–5보다 우선하도록 재현한다.
- [x] 메뉴·게임·지도·엔딩·커튼·GAME OVER 쓰기를 실제 ROM pattern ID가 남는 SCREEN 2 이름 테이블 미러로 전환한다.
- [x] AY 레지스터에서 시작·게임·곡괭이 소리를 결정론적 PCM/WAV로 합성하고 전체 파형 hash 회귀를 추가한다.
- [x] 원본 PSG 음악·효과음을 openMSX 실제 WAV와 직접 비교한다. (시작 음악 260프레임×14 PSG 레지스터 완전 일치, WAV envelope 상관 0.919739, 평균 RMS 비 1.001483, 결정론적 PCM/WAV golden hash 완료)
- [x] 브라우저 캔버스 내부 해상도를 256×192로 유지하고 표시 종횡비를 4:3으로 고정한다.
- [x] 활성 화면을 640×480(2.5배)로 제한하고 원본 MSX 캡처의 좌우 8픽셀 border를 포함한 680×480 표시 래스터를 적용한다.
- [x] 원본 ROM을 openMSX 21.0 C-BIOS MSX1에서 자동 실행해 타이틀, stage 1–15의 진입 화면과 2화면 스테이지의 비진입 방까지 PNG·VRAM·RAM·SAT를 캡처한다. (23개 캡처, 전체 22개 방의 배경 SCREEN 2 색 인덱스 및 visible VRAM 불일치 0, 15개 진입 화면 SAT 합성 framebuffer 불일치 0)
- [ ] 기록된 잔여 항목을 모두 구현·검증한 뒤에만 포팅 완료 상태로 전환한다.

## 진행 로그

### 2026-07-31

- 초기 임시 메뉴(큰 픽셀 글자/도형)를 제거했다.
- ROM 압축 해제 결과로 원본 메뉴를 렌더링했고 `build/title-preview.png`로 256×192 출력 배치를 확인했다.
- ROM 전체 16KB를 주소 보존 자산으로 내보내 음악 스트림을 브라우저에서 읽도록 했다.
- 단순 비프음만 호출하던 경로를 3개 square-wave 채널과 프레임별 음표 진행 경로로 교체했다.
- 브라우저 창 크기에 따라 256×192 캔버스를 정수 배율로 맞추고 내부 해상도를 상태 표시로 명시했다.
- ASM 조립, 자산 검사, 모든 JS 구문 검사, 메뉴 렌더링/음악 스모크 검사를 통과했다.
- 로컬 HTTP 서버에서 `/`, `/src/main.js`, `/src/game/romdata.js` 응답을 확인했다.

### 2026-07-31 — 플레이 화면 자산 보정

- `ChgStoneColor`의 4개 돌 색상 세트를 추출해 스테이지별 벽 색상을 원본 데이터에서 적용했다.
- `FlipPatrones`의 15개 타일 좌우 반전과 `COLOR_Flipped`를 적용했다.
- `GFX_GEMA`를 원본처럼 6회 로드해 보석 패턴 영역을 채웠다.
- 임의 갈색 벽/HUD를 제거하고 실제 패턴·색상·문자 코드로 교체했다.
- 현재 5번 피라미드 캡처에서 원본과 같은 파랑/청록 벽 계열, 흰색 계단, 원본 HUD 위치를 확인했으며, 문·플레이어·모미아·전체 맵 배치는 추가 대조가 필요하다.

### 2026-07-31 — 원본 플레이 화면 비교

- 새 비교 이미지에서 HUD, 화면 배치, 타일 색상, 계단, 문, 아이템, 플레이어/모미아 스프라이트가 현재 포팅과 크게 다름을 확인했다.
- 다음 작업은 메뉴가 아니라 실제 플레이 화면의 MSX SCREEN 2 이름 테이블·패턴·색상 배치를 원본 기준으로 재작업한다.
- 이 비교 작업의 구현 및 검증 결과는 변경되는 즉시 이 문서에 추가한다.

### 2026-07-31 — 플레이어·출구 스프라이트 보강

- 원본 `framesProta`와 `framesMomia` 패턴을 연결하고 주인공을 의상/피부 2중 스프라이트로 렌더링했다.
- 미라 타입별 색상, 프레임 애니메이션, 좌우 반전을 실제 ROM 패턴 기준으로 적용했다.
- 스테이지 입장 위치를 이전 피라미드를 가리키는 출구 좌표에서 계산하도록 바꿨다.
- `animExitOpen`의 실제 5×3 출구 타일 배치를 적용하고 HUD의 `REST`를 원본처럼 두 자리로 표시했다.
- 출구 상태 전이·닫힘 프레임과 Z80의 전체 스프라이트 속성 테이블 동기화는 아직 남아 있다.

### 2026-07-31 — 원본 피라미드 지도 자산 연결

- `gfxMap`, `colorTableMap`, `gfxSprMapa`를 ROM 자산으로 추출해 지도 패턴·색상·이름 테이블을 직접 렌더링한다.
- 임시 `KONAMI PYRAMID MAP` 도형과 숫자 목록을 제거하고 원본 지도 배경, `- Pyramid's Map -` 문구, 피라미드 선택 실루엣/화살표를 연결했다.
- 지도 전환 시간축, 실제 목적지 화살표 상태, 지도에서 게임으로 넘어가는 원본 연출은 아직 남아 있다.

### 2026-07-31 — 통합 검증

- SjASM 재조립 결과 `0 errors`를 확인했다.
- ROM 자산 재생성 결과 SHA-256 `01bd3179540c886ab658626ca0460c329d167327fabd43eb615883375e9e9a3e`, 그래픽 범위 21개를 확인했다.
- 15개 스테이지의 원본 맵 폭, 벽/계단 수, 보석/미라 수를 검사했고 stage 5 입장 좌표 `(232,112)`를 확인했다.
- `web/src` 전체 JavaScript 구문 검사를 통과했다.
- 브라우저 HTTP 캡처에서 실제 Konami 로고가 렌더링되는 것을 확인했고, 비교용 `?mode=play&stage=5`/`?mode=map` 직접 진입 경로를 추가했다.
- `?mode=play&stage=5`의 실제 Edge 캡처(`build/browser-stage5.png`)에서 원본과 같은 256×192 게임 배치, HUD 위치, stage 5 벽/계단/문/아이템 배치를 확인했다. 주인공·미라의 정확한 Z80 속성/색상 합성과 문 상태 시간축은 여전히 원본과 픽셀 단위 대조가 필요하다.
- 원본 `setupStage`의 계단 후처리를 반영해 빈 계단에는 `0x20` 계열, 플랫폼에 닿는 끝에는 `0x15` 계열 ID를 기록하고, 회전문에는 방향별 `0x50/0x52` 계열 ID를 기록하도록 수정했다. 기존처럼 모든 계단/회전문을 같은 패턴으로 반복하지 않는다.
- 출구 상태를 `closed → open → enter` 흐름으로 분리해 보석을 모두 모으기 전 출구 진입이 완료되지 않도록 했고, 보석 수집 후 레버 근처에서 점프/액션할 때 실제 열린 출구 패턴을 표시하도록 했다.
- 연속 추적 카메라를 제거하고 256픽셀 방 단위 목표와 원본처럼 프레임당 4픽셀 스크롤을 적용했다.
- 미라 이동 속도를 원본 `tiposMomia`의 고정소수점 값(`0x50/0xA0/0xB0`)으로 분리하고 초기 방향을 타입 비트가 아닌 ROM 초기화 흐름에 맞춰 오른쪽으로 시작하도록 수정했다. 보석 점수도 원본의 500점으로 맞췄다.
- v2 stage 5 Edge 캡처(`build/browser-stage5-v2.png`)로 변경 결과를 확인했다. 출구/오브젝트 좌표는 안정적이지만 계단 방향과 문·스프라이트 상태 시간축은 원본 비교 대상으로 남겼다.
- 원본 `drawGameLogo`의 22프레임 메뉴 도입을 복원해 `KING'S` 9열, `VALLEY` 13열, 이후 문구와 피라미드 순서로 표시하도록 수정했다.
- 원본 `dibujaLogo`의 14단계·2프레임 상승 좌표를 복원하고, 완료 시 패턴 `0x7A` 밑줄과 `SOFTWARE` 문구를 표시하도록 타이틀 도입을 수정했다.
- 원본 `chkPushAnyKey`를 대조해 메뉴의 `SPACE/FIRE` 입력이 바로 stage 1을 시작하도록 바꾸고, 지도는 스테이지 클리어 후에만 진입하도록 흐름을 수정했다. 클리어 시 원본처럼 목숨도 1 증가시킨다.
- HUD의 HI 값을 고정 0이 아니라 플레이 중 최고 점수로 유지하도록 연결했다.
- 최신 Edge 직접 진입 캡처(`build/browser-debug.png`)에서 stage 5 화면과 ROM 자산 렌더링을 재확인했고, 브라우저 초기화 예외는 상태 표시줄에 남도록 진단 경로를 추가했다.

### 2026-07-31 — ref 하드웨어 계층 이식

- `ref/web`이 에뮬레이터가 아니라 TMS9918 SCREEN 2와 AY-3-8910 상태를 네이티브 JavaScript로 재구현한 구조임을 확인하고, King's Valley에도 같은 분리 방식을 적용하기 시작했다.
- 원본 `setMusic`(`0x7AA9`)·`updateSound`(`0x7B36`)·`processSndData`(`0x7B91`)와 `MusicIndex`(`0x7CF5`)를 기준으로 채널 수, 하위 6비트 우선순위, `0xFE` 패턴 루프, 음악/직접 주파수 두 문법, 프레임별 음량 감쇠를 구현했다.
- 단순 square oscillator 출력 대신 AY DAC 16단계 음량, 17비트 LFSR 노이즈, tone/noise AND 믹싱을 담당하는 WebAudio 백엔드를 추가했다.
- 보석·아이템·곡괭이·칼·미라 충돌·피라미드 이탈 호출을 원본 SFX ID로 교체하는 작업을 진행 중이며, 다음 검증에서 PSG 레지스터 추적을 기록한다.

### 2026-07-31 — 플레이 화면 원본 비교 교정

- 사용자 제공 1번/5번 화면을 다시 기준으로 삼아 `getStairs3`의 `-0x62/-0x60` 이동을 직역했고, 계단 시작 X를 매 행 좌우 1타일씩 이동하도록 수정했다. stage 5 계단 타일은 잘못된 82개에서 원본 루프의 68개로 정정됐다.
- 원본 `flipSprites`가 각 바이트 반전뿐 아니라 16×16 스프라이트의 좌우 8픽셀 블록도 교환한다는 점을 반영해 주인공과 모미아의 좌우 프레임 깨짐을 제거했다.
- 최초 비교에서는 보석·칼·곡괭이의 `+8`을 중복으로 판단해 제거했다. 이후 임시로 동적 요소에 `+8`을 적용했으나, 블록 위치 재검증에서 실제 원인은 압축 맵 0행을 화면에 표시한 것이었음을 확인했다. 최종 구현은 동적 좌표를 원본 값으로 되돌리고 `MapaRAMRoot` 표시행을 바로잡는다.
- 출구 애니메이션 데이터가 패턴 번호가 아니라 맵 ID임을 수정해 `getTileFromID` 변환을 거치도록 했고, 미수집 상태에서는 입구만 보이고 다음 출구는 숨도록 상태를 분리했다.
- 원본 초기 생명 5가 `InitStage`에서 1 감소해 `REST-04`가 되는 흐름을 적용하고 TMS 팔레트 색상 1을 검정으로 수정했다.
- 기본 표시 크기를 640×480으로 제한하고 `?mode=menu` 회귀 경로를 추가했다. Edge 캡처 `build/browser-stage1-match.png`, `build/browser-stage5-match.png`, `build/browser-menu-match.png`에서 캐릭터 패턴, HUD, 문, 계단, 색상, 타이틀 렌더링을 확인했다.

### 2026-08-01 — 원본 프레임 로직 보강

- `StartGame`·`escalerasEntrada`를 기준으로 16프레임 입구 대기, 4프레임마다 `0x01E0` 속도로 좌측 이동하며 8픽셀 하강, 문 닫힘, 40프레임 조작 대기를 구현했다. stage 5 입장 완료 좌표 `(217,120)`을 상태 스모크로 검증했다.
- 원본 수평 속도 `0x00A8`(0.65625px/frame)와 `valoresSalto`의 `[4,2,2,2,1,1,2,0,1,1,0,0]` 정·역방향 점프를 주인공 이동에 적용했다.
- 사망 즉시 시작점으로 순간이동하던 처리를 제거하고, `REST`가 남아 있으면 사망 음악 종료 후 스테이지를 재구성하며 0일 때만 GAME OVER로 전환하도록 수정했다.
- 미라의 초기 16회 반속 림보, 130프레임 구름 등장, 마지막 다리 벌림, 34프레임 폭발, 128회 재등장 대기와 원본 프레임 `0xE8/0xEC/0xD4`를 연결했다. 플랫폼 전방의 벽·바닥 판정도 추가했다.
- 주인공·미라·입구 상태 스모크, 600프레임 인게임 PSG 루프, 즉시 정지 레지스터를 검사했고 모두 통과했다.
- SjASM 재조립 `0 errors`, ROM SHA-256 및 21개 그래픽 자산 검사, 전체 JavaScript 구문 검사를 다시 통과했다.
- Edge 최종 캡처 `build/browser-final-stage5.png`, `build/browser-final-entry.png`, `build/browser-final-menu.png`에서 640×480 표시와 세 화면 경로를 검증했다.

### 2026-08-01 — stage 1 수직 좌표·입구 잔상 재검증

- 사용자 원본/포팅 비교에서 동적 요소가 플랫폼보다 8픽셀 떠 있는 현상을 확인했고, 첫 대응으로 동적 요소 화면 Y를 임시 보정했다.
- 후속 블록 위치 비교에서 원본 `setupStage`가 압축 맵 0행을 `MapaRAMRoot`의 HUD 예약행에 쓰며 `drawRoom`은 다음 행인 `MapaRAM`부터 그린다는 근본 원인을 확인했다. 임시 동적 Y 보정은 다음 절의 최종 수정으로 대체했다.
- 입구의 `open → closing → closed` 애니메이션 뒤 128프레임 시점에 `hidden`으로 전환하고, 비교용 입장 생략 경로도 즉시 `hidden`으로 만들어 중앙 문 절반 잔상을 제거했다.
- 비교용 직접 진입 직후 단순 미라 AI가 주인공과 겹쳐 `REST-03`으로 바뀌지 않도록 180프레임 충돌 보호를 적용하되, 원본 시작 화면 비교에서 주인공이 사라지지 않도록 보호 중에도 스프라이트는 계속 표시한다.
- 256×192 활성 화면 양쪽에 원본 캡처와 같은 내부 8픽셀 검정 border를 추가해 2.5배 표시 기준 활성 화면 640×480, 전체 표시 래스터 680×480으로 맞췄다.
- 216프레임 입장 상태 스모크에서 문이 `hidden`인 채 `active`로 끝나는 것을 확인했고, 전체 JavaScript 구문 검사와 ROM 자산 검사를 통과했다.
- Edge 캡처 `build/browser-stage1-coordinate-final.png`에서 `REST-04`, 주인공·미라의 플랫폼 접지, 중앙 입구 완전 제거, 좌우 표시 여백을 확인했다.

### 2026-08-01 — 타이틀 음악 속도·블록 행 배치 수정

- 원본 `setNote`의 반복 계산은 음표 하위값이 `n`일 때 tempo를 초기 1회와 루프 `n`회 더해 `tempo × (n + 1)` 프레임을 만든다. JavaScript의 `tempo × n` 계산을 수정해 시작 음악이 약 두 배 빠르던 문제를 제거했다.
- `MUS_Start` 재생 종료가 수정 전 156프레임에서 수정 후 260프레임으로 바뀌는 것을 확인하고 자산 검사에 회귀 조건으로 추가했다.
- 원본은 22개 압축 행 중 0행을 HUD 예약행에 쓰고 1–21행만 Y 8–168에 표시하며, 초기화 값 `0x14`로 채운 마지막 행을 Y 176에 표시한다. JavaScript도 같은 행 선택과 하단 패턴 `0x41`을 사용하도록 수정했다.
- 맵 행을 바로잡은 뒤 보석·칼·곡괭이·출구·주인공·미라의 임시 `+8` 화면 보정을 제거하고 원본 데이터/SAT 좌표로 복귀했다.
- 자산 검사에서 stage 1의 Y 8 첫 맵 행이 외벽 2개인지, Y 176 마지막 행이 `0x41` 32개인지 검사한다.
- 전체 JavaScript 구문 검사와 ROM SHA-256/21개 그래픽/15개 스테이지 검사를 통과했고, 결과를 `build/asset-check-map-audio.txt`에 기록했다.
- Edge 캡처 `build/browser-stage1-map-audio-fix.png`에서 수정된 벽·바닥·계단·캐릭터 접지 배치를 확인했다.

### 2026-08-01 — 계단 접점 색상·보석 광택 복원

- 사용자 표시 영역이 모두 계단이 플랫폼에 닿는 특수 타일 `0x15–0x18`임을 확인했다. 패턴 변환은 정확했지만, 정상 방향 패턴 `0x73/0x74`의 색상 바이트가 0이라 검정 구멍처럼 출력되고 있었다.
- 원본 `COLOR_InGame` 압축 스트림은 심볼 경계에서 끝나지 않고 바로 뒤의 `COLOR_Flipped` 데이터까지 진행한 뒤 `0`으로 종료한다. 기존 exporter가 다음 심볼에서 100바이트로 잘라 정상 방향 계단 끝단 색상을 누락한 것이 원인이었다.
- 런타임에서 잘린 기존 자산과 새 완결 자산을 모두 지원하고, exporter가 압축 제어 바이트를 해석해 실제 종료점까지 추출하도록 수정했다. 재추출 검사에서 `COLOR_InGame` 121바이트, `COLOR_Flipped` 21바이트, `COLOR_GEMAS` 13바이트와 각 종료 바이트 `0`을 확인했다.
- 임시로 강제하던 계단·문 색상 덮어쓰기를 제거하고 ROM 색상 스트림만 사용하도록 변경했다. `0x73` 색상 시작값 `EA F9`를 회귀 검사에 추가했다.
- 원본 `putBrillosMap`에 맞춰 각 보석의 위에 `0x51`, 왼쪽에 `0x52`, 오른쪽에 `0x53` 패턴을 배치했다. `drawBrilloGemas`의 `(timer >> 1) & 3`과 `10/F0/A0/A0` 색상 순환을 그대로 적용했다.
- 자산 검사에서 stage 1 첫 보석의 광택이 frame 0에서 `0x10`, frame 2에서 `0xF0`으로 변하는지 검사한다.
- 전체 JavaScript 구문, ROM SHA-256, 21개 그래픽, 15개 스테이지, 시작 음악 260프레임 검사를 통과했고 결과를 `build/asset-check-stairs-gems.txt`에 기록했다.
- Edge 캡처 `build/browser-stage1-stairs-gems-a.png`에서 표시된 계단 접점의 채움/구멍 모양과 네 보석의 광택 배치를 확인했다.

### 2026-08-01 — 보유 아이템·곡괭이·투척 칼 자산 연결

- 원본 `loadAnimation`의 `IndexSprites`를 따라 일반 `GFX_Prota`, 칼 `GFX_ProtaKnife`, 곡괭이 `GFX_ProtaPico`를 별도 VRAM 세트로 해제하고 현재 보유 물건에 맞는 의상/피부 2중 스프라이트를 선택한다.
- 각 아이템 세트에도 16×16 좌우 블록 교환과 비트 반전을 적용해 방향 전환 시 전용 프레임이 깨지지 않도록 했다.
- 곡괭이 사용을 즉시 타일 삭제 방식에서 12프레임 간격의 `0x44 → 0x43 → 공백` 세 타격으로 교체했다. 아래 타일도 굴착 가능한 벽이면 같은 세 단계를 한 번 더 수행하고 아이템을 소모한다.
- 굴착 중 주인공 조작을 잠그고 action frame 8/9를 번갈아 사용하며 각 타격마다 원본 SFX `0x45`를 호출한다. 맵 ID, 충돌 타일, 표시 패턴을 같은 단계에서 갱신한다.
- 투척 칼을 배경 타일 `0x4B`가 아니라 원본 보조 스프라이트의 `0xF0/0xF4/0xF8/0xFC` SAT 패턴과 흰색으로 렌더링하고, 초기 X 오프셋과 수평 속도를 원본의 2px/frame으로 조정했다.
- 비교용 URL에 `item=knife`, `item=pickaxe`, `dig=1`을 추가해 보유 프레임과 굴착 중간 프레임을 직접 캡처할 수 있게 했다.
- 회귀 검사에서 일반/칼/곡괭이 주인공 패턴이 서로 다른지, 칼 `0xF0`이 비어 있지 않은지, stage 1 굴착이 세 번의 `0x45` 타격 후 벽과 아이템을 제거하는지 확인한다.
- 전체 JavaScript 구문과 기존 ROM/음악/맵/보석 검사를 통과했고 결과를 `build/asset-check-items-dig-knife.txt`에 기록했다.
- Edge 캡처 `build/browser-stage1-held-knife.png`, `build/browser-stage1-held-pickaxe.png`, `build/browser-stage1-dig.png`를 생성했다.

### 2026-08-01 — 투척 칼 반사·낙하·바닥 복귀

- 원본 `AI_Cuchillos`의 status 5–8을 `flying → bouncing → falling` 상태로 분리했다. 비행 중에는 X를 2px/frame 이동하고 방향 앞쪽의 플랫폼 또는 회전문 ID를 검사한다.
- 벽 충돌 시 방향을 반전하고 X를 8픽셀 경계에 맞춘 뒤 `parabolaKnife`의 `[-5,-2,-1,0,0,1,2,5]`를 4프레임마다 적용한다.
- 포물선 8단계가 끝나면 4프레임마다 Y를 4px씩 내리고, 플랫폼에 닿으면 화면 칼 엔티티를 제거해 해당 8×8 좌표에 수집 가능한 바닥 칼을 생성한다.
- 회전문·아이템 계열에 떨어지면 포물선 상태를 다시 시작하고, 플랫폼에는 고정되도록 원본 tile family 분기를 적용했다. 보석은 비행을 막지 않는다.
- 비행 상태는 원본 `framesCuchillo`의 `0x45/0x46/0x48/0x49`를 X 4픽셀 위상으로 렌더링하고, 반사·낙하 상태는 `0xF0/0xF4/0xF8/0xFC` 흰색 SAT를 사용한다. 이후 원본 분기 재검증에서 북/남은 1타일, 대각선만 2타일임을 추가로 바로잡았다.
- 합성 맵 회귀 검사에서 비행 칼이 벽에 충돌해 `bouncing`, `falling`을 모두 거친 뒤 Y 72에 한 개의 바닥 칼로 복귀하고, 주인공이 다시 수집해 SFX `0x04`를 내는 것을 확인했다.
- 비교용 `?mode=play&stage=1&item=knife&throw=1` 경로와 Edge 캡처 `build/browser-stage1-knife-flight.png`를 추가했다.
- 입력 즉시 칼을 생성하던 처리를 원본 `accionWaitCnt=0x15` 흐름으로 교체했다. action frame 8에서 5프레임 준비한 뒤 timer 16에 칼과 SFX `0x06`을 생성하고 action frame 9로 바꾸며, 나머지 16프레임 뒤 일반 무장 상태로 돌아간다.
- 투척 자세 동안 주인공 이동과 반복 투척을 잠그고, 칼이 방출된 뒤에도 애니메이션이 끝날 때까지 칼 보유 전용 그래픽을 유지한다.
- 회귀 검사에서 준비 4프레임까지 칼이 생성되지 않고 5번째 갱신에 정확히 한 개가 생성되는지, 전체 21프레임 뒤 아이템과 action 상태가 해제되는지 확인했다.
- Edge 캡처 `build/browser-stage1-knife-prepare.png`, `build/browser-stage1-knife-released.png`에서 투척 자세와 비행 칼을 확인했다.
- 전체 자산·음악·맵·아이템·굴착·칼 상태 검사를 통과했고 결과를 `build/asset-check-knife-complete.txt`에 기록했다.

### 2026-08-01 — 주인공 계단 상·하행 복원

- 기존 구현은 계단을 수직 사다리로 취급해 중앙 한 점에서 `UP/DOWN`으로 Y만 이동했기 때문에 45도 계단의 타일을 즉시 벗어나 상행할 수 없었다.
- 원본 `chkCogeEscalera`처럼 진입 시에만 `UP/DOWN`을 검사하고, `X mod 8`에 따라 `0x21/0x22` 몸체 또는 `0x16/0x17` 끝점 ID를 판정한다. 첫 계단 높이는 `distPeldano = [0,-1,-2,-3,-4,-3,-2,-1]`로 보정한다.
- 계단 상태에서는 원본과 같이 `LEFT/RIGHT`로 2프레임마다 X와 Y를 각각 1픽셀 이동한다. `0x20/0x21` 계단은 왼쪽이 상행, `0x22/0x23` 계단은 오른쪽이 상행이다.
- X가 4픽셀 경계이고 Y가 8픽셀 경계일 때 `(X+8,Y+16)`의 맵 ID가 `0x10` 계열이면 보행 상태로 돌아가도록 끝점 판정을 구현했다.
- 보행·착지 판정도 원본 `chkPisaSuelo`의 `(X+6,Y+17)`, `(X+10,Y+17)` 두 점으로 맞춰 계단 끝에서 캐릭터가 뜨거나 2픽셀 내려앉지 않도록 했다.
- stage 1 회귀 검사에서 우상향 계단은 `(60,112) → (96,72)`, 같은 계단 하행은 `(92,72) → (56,112)`, 좌상향 계단은 `(45,72) → (16,40)`으로 진입·이탈하는지 확인한다.
- 전체 JavaScript 구문과 ROM/그래픽/15개 스테이지/음악/맵/아이템/칼 검사를 통과했고 결과를 `build/asset-check-stairs-movement.txt`에 기록했다.
- 디버그 경로 `?mode=play&stage=1&stair=up`과 Edge 캡처 `build/browser-stage1-stair-movement.png`에서 계단 중간의 주인공 위치와 경사를 확인했다.

### 2026-08-01 — 미라 계단 탐색·추적 이동

- 원본 `buscaCercanias`와 `buscaProta`를 따라 미라와 주인공의 높이 차이를 계산하고, 현재 위치 좌우 5타일 안에서 벽으로 막히지 않은 상행 또는 하행 계단 진입점을 찾는다.
- 주인공과 중복되던 `X mod 8`, `distPeldano`, `0x21/0x22`, `0x16/0x17`, 끝점 `0x10` 계열 판정을 공용 액터 계단 함수로 분리해 미라에도 같은 좌표 규칙을 적용했다.
- 미라 보행과 계단 이동을 별도 상태로 나누고, 계단에 진입하면 경사 방향에 따라 좌·우 제어를 바꾸며 끝점에서 보행 판단을 다시 시작한다.
- 원본 `pausaEscalera = [3,0,1,0,3]` 마스크를 적용해 타입 0/4는 4프레임마다, 타입 2는 2프레임마다, 타입 1/3은 매 프레임 한 픽셀씩 이동한다.
- stage 1 회귀 검사에서 타입 2 미라는 81프레임에 `(56,112) → (96,72)` 상행하고, 타입 0 미라는 159프레임에 `(88,72) → (56,112)` 하행하는지 확인한다.
- 전체 JavaScript 구문과 ROM/그래픽/15개 스테이지/음악/맵/아이템/계단/칼 검사를 통과했고 결과를 `build/asset-check-mummy-stairs.txt`에 기록했다.
- 비교 경로 `?mode=play&stage=1&mummyStair=up`과 Edge 캡처 `build/browser-stage1-mummy-stair.png`에서 미라 스프라이트가 계단 경사 위에 배치되는 것을 확인했다.
- 미라의 타입별 망설임 횟수, 장애물 우회, 점프/낙하 선택, 스트레스 누적과 자폭은 다음 미완료 게임 로직으로 유지한다.

### 2026-08-01 — 미라 점프·플랫폼 낙하

- 원본 `momiaSinSuelo` 분기대로 타입 0/3 미라와 플레이어보다 위에 있는 미라는 플랫폼 끝에서 떨어지고, 타입 1/2/4 미라는 위쪽 공간이 확보되면 진행 방향으로 점프한다.
- `valoresSalto = [4,2,2,2,1,1,2,0,1,1,0,0]`을 상승 시 음수, 하강 시 역순 양수로 적용하고 표가 끝난 뒤에는 매 프레임 4픽셀 최대 낙하로 전환한다.
- 보행·계단·점프·낙하를 독립 상태로 분리했다. 낙하 시작 시 원본 `chkCae`처럼 X를 4픽셀 경계에 맞추고, 착지 시 Y를 8픽셀 경계에 맞춘 뒤 보행 판단을 다시 시작한다.
- 전방의 짧은 벽에 닿으면 머리 두 점과 진행 방향의 위쪽 공간을 검사해 점프하고, 공간이 막혀 있으면 방향을 반전한다. 점프 중에는 전방 상·중·하단과 발밑 플랫폼을 검사한다.
- 합성 24픽셀 간격 맵 회귀 검사에서 타입 0 미라는 `walking → falling → walking`으로 아래 Y 104 플랫폼에 착지하고, 타입 2 미라는 `walking → jumping → walking`으로 간격을 건너 원래 Y 64 플랫폼에 착지한다.
- 전체 자산·음악·맵·계단·아이템·칼 검사를 통과했고 결과를 `build/asset-check-mummy-jump.txt`에 기록했다.
- 비교 경로 `?mode=play&stage=1&mummyJump=1`과 Edge 캡처 `build/browser-stage1-mummy-jump.png`에서 점프 중 미라 스프라이트와 화면 좌표를 확인했다.
- 타입별 망설임, 장거리 장애물 우회, 충돌 스트레스 누적과 자폭은 다음 미완료 AI 단계로 유지한다.

### 2026-08-01 — 미라 망설임·충돌 스트레스

- 원본 `momiaAnda`처럼 미라는 결정 후 `ACTOR_TIMER=5`로 시작해 16프레임마다 한 번 감소하며 총 80프레임 동안 보행한 뒤 다시 생각 상태로 들어간다.
- `vecesDudaMomia = [3,3,0,0,3]`을 적용해 타입 0/1/4는 32프레임 간격으로 세 번 좌우를 보고, 타입 2/3은 다음 프레임에 즉시 플레이어와 계단 위치를 다시 판단한다.
- 생각 중에는 원본처럼 다리를 벌린 frame 2를 유지하고 남은 망설임 카운터의 홀짝에 따라 좌우 방향을 번갈아 표시한다.
- 벽 충돌 시 스트레스 값을 `((stress & 0xF0) + 0x1F) & 0xFF`로 누적하고, `0xAF`에 도달하면 34프레임 폭발 상태로 전환한 뒤 기존 림보·재등장 흐름으로 복귀한다.
- 충돌 없이 이동하는 동안에는 32프레임마다 스트레스를 한 단계 감쇠해 원본의 연속 충돌 판정을 유지한다.
- 회귀 검사에서 타입 0 미라가 96프레임 동안 세 번 망설인 뒤 `walkTimer=5`로 보행을 시작하고, 초기 `0xF0`에서 11번째 연속 충돌에 `phase=exploding`, `timer=0x22`가 되는지 확인한다.
- 생각 상태 추가에 따른 전체 경로 시간도 갱신해 타입 2 계단 상행은 83프레임, 타입 0 계단 하행은 255프레임에 끝나는지 검사한다.
- 전체 JavaScript 구문과 자산·음악·맵·계단·점프·아이템·칼 검사를 통과했고 결과를 `build/asset-check-mummy-ai.txt`에 기록했다.
- Edge 캡처 `build/browser-stage1-mummy-ai.png`에서 두 미라의 생각/보행 프레임과 정상 게임 화면 렌더링을 확인했다.
- `evitaSorpresa`의 화면 경계 기습 방지, 양쪽 벽에 갇힌 타입 0의 특수 생각 상태, `LD A,I` 기반 무작위 장애물 우회는 미완료 예외 AI로 유지한다.

### 2026-08-01 — 투척 칼 비행 프레임 깨짐 수정

- 비행 칼이 회전 중 두 조각으로 갈라져 보이는 현상을 원본 `movKnife`의 X 4픽셀/8픽셀 분기와 비교했다.
- 기존 렌더러는 `0x45/0x46/0x48/0x49` 네 프레임 모두에 무조건 `pattern + 1`을 오른쪽 타일로 추가해, 1타일 프레임 옆에 다음 회전 프레임 조각을 잘못 붙이고 있었다.
- 원본은 X가 8픽셀 배수인 `0x45`, `0x48` 북/남 위상에서는 한 타일만 쓰고, X가 `+4`인 `0x46+0x47`, `0x49+0x4A` 대각선 위상에서만 두 타일을 연속으로 쓴다.
- X가 2픽셀씩 이동하는 중간 프레임은 직전 `(X >> 2) & 3` 위상을 유지하도록 `knifeFlightFrame`으로 패턴, 타일 X, 타일 수를 함께 계산한다.
- X 0/2/4/6/8/10/12/14의 8개 위치에서 패턴 `45/45/46/46/48/48/49/49`, 타일 수 `1/1/2/2/1/1/2/2`를 검사한다.
- 전체 JavaScript 구문과 기존 자산·게임 로직 검사를 통과했고 결과를 `build/asset-check-knife-flight-frame.txt`에 기록했다.
- 정지 비교 경로 `?mode=play&stage=1&knifePhase=0..3`과 캡처 `build/browser-stage1-knife-phase-0.png`부터 `build/browser-stage1-knife-phase-3.png`까지 네 위상이 온전하게 표시되는지 확인했다.

### 2026-08-01 — 회전문 밀기·통과 상태기

- 원본 `setupStage`를 다시 대조해 회전문 세로 행마다 ID가 감소하는 기존 해석을 제거했다. 모든 행은 방향 8에서 `0x50/0x51`, 방향 4에서 `0x52/0x53`의 같은 좌우 쌍을 반복한다.
- 주인공 수평 충돌 지점의 맵 ID를 보존하고, `protaAnda`처럼 회전문 계열 중 낮은 니블 0/3 쪽만 밀 수 있으며 1/2 쪽에서는 밀기 카운터가 초기화되도록 했다.
- 같은 회전문을 16프레임 연속 밀면 SFX `0x03`과 회전을 시작한다. 통과 중에는 일반 충돌을 잠그고 2프레임마다 원본 X 속도 `0xA8`을 32번 적용해 64프레임 동안 21픽셀 이동한다.
- `spiningDoors`의 8프레임 간격과 `0x68/69 → 0x6A/6B → 0x54/55 → 0x7A/79 → 0x78/77` 다섯 표시 단계를 적용하고, 종료 시 방향을 `XOR 0x0C`로 뒤집어 정적 맵 ID를 갱신한다.
- 15개 스테이지의 모든 회전문 행 ID, 막힌 면 거부, 16프레임 진입, 통과 거리, 다섯 프레임 순서와 종료 방향을 `tools/check_kingsvalley_assets.mjs`에서 검사한다.
- 비교 경로 `?mode=play&stage=4&spinner=mid`를 추가하고 Edge 캡처 `build/browser-stage4-spinner-mid.png`에서 ROM 패턴 `0x54/0x55`의 회전 중간 프레임을 확인했다.
- 전체 JavaScript 구문과 기존 자산·게임 로직 검사를 통과한 결과는 `build/asset-check-spinner.txt`에 기록한다.

### 2026-08-01 — 함정 벽 발동·하강 상태기

- 스테이지 데이터의 함정 좌표는 처음부터 놓이는 `0x19` 벽이 아니라 주인공이 정확히 통과할 때 쓰는 발동 좌표다. 기존 정적 벽 삽입을 제거해 막혀 있던 통로와 덮였던 stage 2 계단 타일을 원본 맵으로 복구했다.
- 발동 시 같은 열을 위로 검색해 처음 만난 벽을 기점으로 삼고, 천장까지 비어 있으면 숨은 0행에 플랫폼 ID `0x12`를 만든 뒤 하강을 시작한다.
- 원본 `MurosTrampa`처럼 전역 타이머 32프레임마다 4픽셀 이동하며, 중간 위치에는 전체 함정 벽 `0x19/0x40`, 8픽셀 경계에는 반 벽 `0x1A/0x42`를 쓴 뒤 다음 단계에서 전체 벽으로 채운다.
- 보석·바닥 칼·곡괭이가 다음 셀을 차지하면 4픽셀을 되돌려 물체가 없어질 때까지 대기하고, 플랫폼 `0x10` 계열 또는 하단 경계에 닿으면 닫힘 상태로 종료한다.
- 일반 보행 상태인 주인공의 머리 좌우 `(X+5,Y)`, `(X+11,Y)`가 새 `0x19/0x1A` 셀에 들어오면 SFX `0x1D`와 기존 잔기/재시작 흐름을 호출한다.
- stage 5 회귀 검사에서 초기 발동 셀이 비어 있는지, Y 144 천장을 찾는지, 전체/반 타일 순서, 바닥 칼 대기, Y 176 종료와 Y 160 압사 시점을 확인한다.
- 비교 경로 `?mode=play&stage=5&trap=mid`와 Edge 캡처 `build/browser-stage5-trap-mid.png`에서 하강 중 벽과 반 타일 끝단을 확인했다.
- 전체 JavaScript 구문과 기존 자산·게임 로직 검사를 통과한 결과는 `build/asset-check-trap.txt`에 기록한다.

### 2026-08-01 — 보석 구멍·피격 칼·단일 액션 수정

- stage 1 하단 보석이 벽 타일 위에 그림으로만 겹쳐 있어 주인공 충돌은 그대로 막히는 문제를 원본 `putNextGema`와 `putBrillosMap`에서 추적했다.
- 보석 배치 시 위 `0x40`, 왼쪽 `0x41`, 중앙 `0x40 + 색상`, 오른쪽 `0x42`를 맵에 기록하고 네 셀을 비고체로 바꾼다. 수집 시 원본 `gemaCogida`처럼 네 셀을 모두 0으로 지운다.
- stage 1의 `(16,160)` 보석에서 막혀 있던 벽 세 칸과 위쪽 1타일 입구가 열리며, `(18,136)`에서 왼쪽으로 이동한 주인공이 입구로 떨어져 보석 판정 범위에 도달하는지 회귀 검사한다.
- 원본 `movKnife8 → movKnife11 → knifeChoca`를 따라 적 피격 시 칼을 삭제하지 않고 `collided` 상태로 전환한다. 다음 프레임에 방향을 뒤집고 8픽셀 경계에 정렬한 뒤 기존 포물선·낙하·바닥 칼 복귀 상태기를 수행한다.
- 합성 맵에서 적을 맞힌 칼이 `collided → bouncing → falling`을 거쳐 `(64,72)`의 수집 가능한 바닥 칼로 돌아오며, 100점과 SFX `0x08`을 한 번 적용하는지 검사한다.
- `Input`의 별도 FIRE/Z 경로를 제거했다. SPACE는 현재 아이템이 칼이면 투척, 곡괭이면 굴착, 아이템이 없으면 점프로 분기하며 Enter와 Shift도 원본 액션에서 제외했다.
- 브라우저 캡처 `build/browser-stage1-gem-pit-fix.png`에서 하단 보석 구멍을, `build/browser-stage1-knife-hit-bounce.png`에서 미라 폭발과 반동 중 칼을 확인했다.
- 전체 JavaScript 구문과 자산·게임 로직 검사를 통과한 결과는 `build/asset-check-three-gameplay-fixes.txt`에 기록한다.

### 2026-08-01 — 바닥 칼·곡괭이 맵 점유

- 바닥 아이템을 별도 그림으로만 출력하던 구현을 원본 `initCuchillo`, `getPicos3`, `chkCogeKnife4`, `chkCogePico`와 대조했다. 원본은 아이템도 이름 테이블 맵 ID를 실제로 점유한다.
- 바닥 칼은 놓이는 셀의 맵 ID·충돌 종류·패턴을 백업한다. 일반 셀에는 `0x30`, `0x21/0x22` 계단에는 `0x31/0x32`를 기록하고 수집 시 백업한 계단 또는 빈 셀을 그대로 복원한다.
- 적 피격이나 벽 반동 뒤 착지한 칼도 같은 배치 경로를 사용해 함정 벽과 다른 투척 칼이 감지할 수 있으며, 다시 수집하면 배경이 복원된다.
- 곡괭이는 원본처럼 기존 셀을 `0x80` 비고체 셀로 치환한다. stage 6 `(480,168)`처럼 원래 벽이던 위치도 막히지 않으며 수집 시 맵 ID 0으로 지운다.
- 이미 칼이나 곡괭이를 든 상태에서는 다른 바닥 아이템을 수집하지 않도록 `collectItems` 진입에서 차단하고, 한 프레임에 칼과 곡괭이를 연속 수집하던 문제를 제거했다.
- 비행 칼의 장애물 계열에 `0x30` 칼과 `0x80` 곡괭이를 추가했다. 보석 `0x40`은 원본처럼 통과한다.
- 회귀 검사에서 빈 셀 칼 점유·복원, 계단 `0x21 → 0x31 → 0x21`, stage 6 벽 곡괭이 `0x12 → 0x80 → 0`, 보유 중 수집 차단과 비행 칼의 곡괭이 반동을 확인한다.
- 비교 경로 `?mode=play&stage=6&itemMap=pick`과 Edge 캡처 `build/browser-stage6-pick-map.png`에서 오른쪽 방 하단 곡괭이와 제거된 벽 충돌 셀을 확인했다.
- 전체 JavaScript 구문과 자산·게임 로직 검사를 통과한 결과는 `build/asset-check-item-map.txt`에 기록한다.

### 2026-08-01 — 곡괭이 옆 벽·제한 분기

- 원본 `chkProtaPica`처럼 먼저 주인공이 단단한 바닥에 서 있는지 검사한다. 일반 상태에서는 방향과 `X mod 8`에 따라 발밑 앞쪽 굴착 셀을 선택한다.
- 굴착 셀 한 줄 위가 빈칸 또는 계단 `0x20` 계열일 때만 시작해 보석·칼·곡괭이 위의 바닥을 파지 못하게 했고, 바로 아래가 회전문 `0x50` 계열이면 시작하지 않는다.
- 양쪽 `X`와 `X+16`의 머리/몸통 높이가 벽으로 막힌 상태에서는 `picaLateral`로 전환한다. 바라보는 옆 벽의 위쪽이 벽이면 위·아래 두 셀, 위가 비고 아래만 벽이면 아래 한 셀만 파괴한다.
- 각 방의 로컬 X `0x00/0xF8` 외벽은 옆 굴착 대상에서 제외했다. 오른쪽 일반 굴착과 왼쪽 `X mod 8` 보정도 원본 좌표 분기에 맞췄다.
- VERSION2의 `drawAgujero4`처럼 완전히 파괴된 셀은 0이 아니라 비고체 맵 ID `1`로 남겨 함정 벽의 정지 판정과 구분되도록 했다.
- 합성 회귀 맵에서 옆 벽 두 셀은 7회의 SFX 호출을 포함해 모두 ID `1`, 아래 한 셀은 3회의 호출 후 아래만 ID `1`이 되는지 확인한다. 아이템 위와 회전문 위의 바닥은 곡괭이가 소비되지 않고 시작도 하지 않는다.
- 비교 경로 `?mode=play&stage=1&dig=lateral`과 Edge 캡처 `build/browser-stage1-lateral-dig.png`에서 stage 1 하단 오른쪽의 옆 벽 굴착 자세와 파괴 타일을 확인했다.
- 전체 JavaScript 구문과 자산·게임 로직 검사를 통과한 결과는 `build/asset-check-lateral-dig.txt`에 기록한다.

### 2026-08-01 — 곡괭이 원본 타이밍·끼임 보정

- 원본 `protaPicando`의 8비트 action counter를 프레임 단위로 직역했다. 하위 니블이 0이 될 때 자세 유지 시간을 4/8프레임으로 교대하고 bit 4를 반전하며, 실제 타격 프레임에서만 구멍 카운터를 3씩 줄인다.
- 파괴 패턴 순서를 ROM의 `tilesAnimCavar`와 같은 `0x43 → 0x44 → 0`으로 수정했다. 위 셀은 구멍 카운터 `0x0c`, 아래 셀은 `0x03`에서 비고체 맵 ID `1`로 전환한다.
- 일반 한 셀 굴착은 56프레임과 20/32/44프레임 타격, 옆 벽 두 셀은 74프레임, 아래 한 셀은 26프레임이 걸리도록 원본 카운터를 재현했다.
- 굴착 종료 시 원본 `chkIncrust`와 같은 좌측 `x+4`/우측 `x+11`, `y+12/y+4/y-4` 충돌 검사를 수행한다. 벽에 끼었으면 X를 4픽셀 격자에 맞춰 좌우로 보정한다.
- 전용 회귀 검사에 패턴 전환 프레임, SFX 횟수, 두 종류 옆 굴착 종료 시간과 좌우 끼임 보정 좌표를 추가했다. 전체 검사는 통과했으며 결과를 `build/asset-check-dig-timing.txt`에 기록했다.
- 비교 경로 `?mode=play&stage=1&dig=lateral`과 Edge 캡처 `build/browser-stage1-lateral-dig-timing.png`에서 첫 타격 패턴과 주인공 자세를 확인했다.

### 2026-08-01 — 미라 방 경계·막힌 통로 우회

- 원본 `evitaSorpresa`의 방 번호와 로컬 X 검사를 이식했다. 미라가 인접 방의 좌우 80픽셀 경계에 있고 플레이어도 맞닿은 쪽 80픽셀 안에 있으면, 미라는 경계를 넘어 기습하지 않고 반대 방향으로 돌아선다.
- 원본 `momiaDecide5`처럼 같은 높이의 플레이어까지 최대 32개 맵 셀을 훑어 `0x10` 벽 계열이 사이를 막는지 검사한다.
- 막힌 통로에서는 5타일 반경 계단 후보 중 내려가는 길을 우선 선택한다. 내려갈 길이 없을 때만 원본 `I` 레지스터 bit 1 분기를 게임 타이머 bit 1로 재현해 올라가는 길을 확률적으로 선택한다.
- 서로 다른 높이에서는 기존의 위/아래 계단 탐색을 그대로 사용하고, 같은 거리 후보는 현재 진행 방향을 우선해 원본의 양방향 탐색 순서를 보존한다.
- 경계 양방향 반전·안전 범위 비반전·벽 검출·하강 우선·상승 선택/거부를 회귀 검사에 추가했다. 기존 미라 계단 시간, 점프/낙하, 스트레스와 전체 자산 검사를 통과했으며 결과는 `build/asset-check-mummy-routing.txt`에 기록했다.
- 실제 다중 방 통합은 `?mode=play&stage=2`와 Edge 캡처 `build/browser-stage2-mummy-routing.png`로 확인했다.

### 2026-08-01 — 출구 개방·퇴장·클리어 전환

- 보석을 모두 모으면 닫힌 출구를 표시하고, 원본 `chkOpenExit`처럼 별도 액션 키가 아니라 점프 중 레버 접촉으로 문을 열도록 수정했다.
- `openCloseExit`의 32프레임 간격을 적용해 닫힘→중간→열림을 96프레임에 진행하고, 퇴장 시 열림→중간→닫힘 뒤 128프레임에 문 상태를 확정한다. 개폐 효과음 `0x8d`도 원본 중간 프레임에서만 재생한다.
- 열린 출구에 닿으면 미라를 숨기고 주인공을 4프레임마다 오른쪽 1.875픽셀·위쪽 1픽셀 자동 이동시킨다. 문의 우측 두 타일 열을 주인공 뒤가 아니라 앞에 다시 합성하고 문 안으로 들어가면 주인공을 숨긴다.
- 문이 완전히 닫힌 뒤 첫 클리어에만 2000점을 더하고 `0x8f` 팡파르를 재생하며 112프레임 대기한다. 기존 임의 `PYRAMID CLEAR` 검은 상자 표시는 제거했다.
- 선택한 출구의 ROM `target`을 `destinationStage`로 전달해 단순 `stage + 1` 대신 실제 목적지 피라미드로 이동한다. 완료 피라미드는 `GameState.clearedStages`에 기록한다.
- 회귀 검사에서 개방 96프레임, 퇴장 전체 240프레임, 개폐/팡파르 호출, 2000점과 stage 1→2 목적지를 확인했다. 전체 결과는 `build/asset-check-exit-sequence.txt`에 기록했다.
- 비교 경로 `?mode=play&stage=1&exit=depart`와 Edge 캡처 `build/browser-stage1-exit-departure.png`에서 열린 문, 상승 중 주인공과 우측 문 가림을 확인했다.

### 2026-08-01 — 피라미드 지도·검은 커튼 시간축

- ROM 문 배열의 네 슬롯을 위·아래·왼쪽·오른쪽 출구 방향 `1/2/4/8`로 보존하고, 문 데이터의 하위 니블은 목적지 입구 방향으로 별도 유지한다.
- 원본 `tickPergamino`처럼 지도 진입 후 88프레임 동안 현재 피라미드와 나가는 화살표를 표시하고, 88프레임에 목적지 피라미드와 반전된 입구 화살표로 전환한 뒤 224프레임에 지도를 종료한다.
- 화살표는 ROM 스프라이트 `0xE8/0xEC/0xF0/0xF4`, 원본 `offsetFlechas`, 일반/반전 번호표를 방향별로 적용한다. 15번에서 1번으로 이동하면 피라미드 실루엣 대신 GOAL 좌표 `(0x7D,0x90)`에 화살표를 표시한다.
- 지도 종료 시 32개 세로 타일 열을 프레임마다 하나씩 왼쪽에서 오른쪽으로 검게 덮은 뒤 목적지 스테이지를 시작한다. 같은 커튼을 메뉴 첫 시작과 사망 재시작에도 연결했다.
- GOAL에서는 지도 음악 종료 후 128프레임을 기다린 뒤 엔딩 상태로 전환한다. 원본의 special bonus 10,000점은 지도에서 미리 더하지 않고 엔딩의 자동 보행·점프가 끝난 뒤 처리한다.
- 회귀 검사에 문 슬롯 방향, 화살표 패턴·오프셋, 87→88프레임 강조 이동, 224프레임 종료와 GOAL 엔딩 전환을 추가했다. 전체 결과는 `build/asset-check-map-transition.txt`에 기록했다.
- 브라우저 캡처 `build/browser-map-stage1-origin.png`, `build/browser-map-stage2-destination.png`, `build/browser-map-curtain-16.png`에서 현재/목적지 화살표와 16열 커튼을 확인했다.

### 2026-08-01 — 방 사이 8프레임 타일 스크롤

- 원본 `chkScroll`의 오른쪽 로컬 X `0xF4`, 왼쪽 로컬 X `<2` 경계에서만 인접 방 스크롤을 시작한다. 외곽 방에서는 기존 월드 경계를 유지한다.
- 기존 카메라의 프레임당 4픽셀·64프레임 보간을 제거했다. VERSION2 `tickScroll`처럼 프레임마다 4타일(32픽셀)씩 이름 테이블 표시 시작점을 옮겨 정확히 8프레임에 256픽셀 방 하나를 전환한다.
- 스크롤 중에는 입력·미라·함정 등 게임 상태 갱신을 정지하고 주인공·미라·낙하 칼 스프라이트를 숨긴다. 이름 테이블 기반 벽·계단·보석·바닥 아이템·비행 칼은 두 방의 합성 위치에 그대로 표시한다.
- 오른쪽 전환 완료 시 주인공을 다음 방 X `4`, 왼쪽 완료 시 이전 방 X `0xF0`에 재배치해 원본 `ScrollPantalla`의 경계 복귀 좌표를 적용한다.
- 합성 회귀 검사에서 오른쪽 4프레임 카메라 `128`, 8프레임 카메라 `256`/주인공 `260`, 왼쪽 8프레임 카메라 `0`/주인공 `240`을 확인했다. 결과는 `build/asset-check-room-scroll.txt`에 기록했다.
- 비교 경로 `?mode=play&stage=2&scroll=mid&freeze=1`과 캡처 `build/browser-stage2-room-scroll-mid.png`에서 두 방이 16타일씩 표시되고 동적 스프라이트가 숨겨진 중간 프레임을 확인했다.

### 2026-08-01 — AY 레지스터 프레임 추적

- 원본 `WRTPSG` 호출 순서에 맞춰 tone period 상위 레지스터를 하위 레지스터보다 먼저 쓰고, mixer `R7`을 포함한 AY-3-8910 16개 레지스터의 값을 보존한다.
- 음악과 효과음의 매 프레임 레지스터 쓰기 순서와 전체 스냅샷을 기록하는 `beginTrace`, `endTrace`, `snapshot` 경로를 추가하고 `tools/trace_kingsvalley_psg.mjs`에서 JSON으로 내보낸다.
- 원본 8비트 duration이 `0`일 때 1프레임이 아니라 256프레임 뒤 wrap되는 동작을 복원해 장음과 효과음 길이 오차를 제거했다.
- 시작 음악 `0x97` 260프레임과 게임 음악 `0x8B` 600프레임의 golden hash, 3채널 음악 우선순위, 채널 3 효과음 우선순위, 곡괭이 noise/mixer 첫 프레임을 회귀 검사에 고정했다.
- 브라우저 `?soundTrace=1` 상태 표시에서 실시간 AY `R0–R10` 값을 확인했고, 결과와 샘플 추적은 `build/browser-stage1-psg-registers.png`, `build/psg-trace-start.json`, `build/psg-trace-pickaxe.json`에 기록했다.
- 레지스터·스트림 자동 대조를 완료했다. 이후 openMSX 실제 PSG snapshot과 WAV 직접 비교까지 수행했으며 상세 결과는 아래 직접 비교 기록에 남겼다.

### 2026-08-01 — 원본 사막 엔딩·SPECIAL BONUS

- 기존 임의 별 배경과 `KING'S VALLEY / PRESS ANY KEY` 화면을 제거하고, ROM `endingTiles`, `endingColors`, `tilesEndDoor`, `starsLocations`, `protaEndingDat`를 자산 내보내기에 추가했다.
- 원본 `drawHalfPiram` 주소를 이름 테이블로 직역해 오른쪽 대형 피라미드, 왼쪽 중형 피라미드, 중앙 소형 피라미드, 3×2 문, 모래 바닥과 실제 6개 별을 배치했다.
- GOAL 지도 음악 종료 뒤 128프레임 대기 시 점수를 미리 더하던 오류를 제거했다. 32프레임 커튼 뒤 게임 음악 `0x8B`를 시작하고 곡괭이를 든 주인공을 `(240,136)`에 배치한다.
- 녹화 입력 `0x88`프레임 좌측 이동과 `0x1B`프레임 좌측+점프, 0.75px 고정소수점 속도, 이어지는 수직 변위 4배 점프를 재현해 `(117,136)`과 착지 `(94,136)`을 검증했다.
- 착지 다음 프레임에 생명 1개, 효과음 `0x8A`, `CONGRATULATIONS / SPECIAL BONUS  10000`과 10,000점을 적용하고 208프레임 뒤 생명 보정·무음·1번 피라미드 재시작 커튼으로 전환한다.
- 이름 테이블, 163프레임 보행, 23프레임 확대 점프, 보너스와 재시작을 `build/asset-check-ending.txt`로 검증하고 `build/browser-ending-special-bonus.png`에서 브라우저 출력을 확인했다.

### 2026-08-01 — 메뉴 무입력 stage 5 데모

- 원본 `WaitMainMenu`의 32프레임 대기를 적용해 완성된 타이틀에서 입력이 없으면 검은 커튼 뒤 자동 데모로 전환한다. 방향키만 누르면 원본의 8비트 `waitCounter=0` 감소처럼 다음 데모까지 256프레임으로 연장한다.
- `DemoKeyData` 62바이트를 ROM 자산으로 내보내고, 외부 초기 유지값 8과 각 `(controls, next duration)` 쌍을 해석해 종료 표식까지 정확히 2,000프레임을 재생한다.
- 데모는 원본 `piramideDest=5`, `puertaEntrada=8`에 맞춰 stage 5 오른쪽 입구와 REST 04로 시작하며, 수동 플레이와 달리 입장 완료 뒤 게임 음악 `0x8B`를 재생하지 않는다.
- SPACE 비트의 상승 에지만 8회 액션으로 전달해 점프·아이템 사용을 일반 `Level` 로직에서 그대로 실행하고, 녹화 종료·피격 또는 실제 키 입력 시 원본 타이틀 흐름으로 복귀한다.
- 새 게임 시작 시 stage·score·lives·완료 피라미드를 초기화해 데모나 이전 게임 상태가 다음 플레이에 남지 않도록 했다.
- 첫 입력 전환, 전체 2,000프레임, 8회 액션, 32프레임 메뉴 대기와 데모 무음 조건을 `build/asset-check-demo.txt`로 검증하고 `build/browser-stage5-demo-frame240.png`에서 stage 5 자동 플레이를 확인했다.

### 2026-08-01 — 원본 GAME OVER 오버레이

- 기존 전체 검은 화면, 확대 적색 글자와 `PRESS ANY KEY`를 제거했다. 원본처럼 마지막 플레이 화면을 동결하고 이름 테이블 `(9,9)`부터 12×5 타일만 검게 비운다.
- `TXT_GameOver`의 VRAM 주소 `0x396B`를 화면 `(88,88)`로 변환해 ROM 글꼴 코드 `GAME OVER` 9자를 원본 크기로 표시한다.
- 마지막 피격 즉시 게임오버 음악으로 덮지 않고 사망 음악 `0x1D`의 3채널이 끝날 때까지 `dying` 상태에서 기다린 뒤 `0x9A`를 시작한다.
- `waitCounter=0xB8`을 원본 `tickGameOver`처럼 2프레임마다 한 번 감소시키고, 종료 시 입력이 없으면 Konami 도입부로, 종료 프레임에 입력이 있으면 완성된 메뉴로 복귀한다.
- 사망 음악 대기, 게임오버 음악, 184회 반속 카운터와 자동 타이틀 복귀를 `build/asset-check-gameover.txt`로 검증하고 `build/browser-stage5-gameover.png`에서 오버레이 위치를 확인했다.

### 2026-08-01 — F1 PAUSING 점멸

- 기존 화면 중앙의 검은 `PAUSE` 박스를 제거하고 `txtPAUSING` 문자 코드 7자를 VRAM `0x3AF6`, 화면 `(176,184)`에 원본 8×8 글꼴로 표시한다.
- 원본 `blinkPausing`의 timer bit 4를 따라 16프레임 표시·16프레임 숨김을 반복하고, F1으로 해제하는 프레임에는 즉시 지운다.
- 게임뿐 아니라 피라미드 지도에서도 같은 F1 플래그로 상태 갱신을 정지한다. 최초 정지 키 프레임과 해제 키 프레임에는 원본처럼 해당 프레임의 게임 로직을 한 번 실행한다.
- `tickMain`이 정지 여부보다 먼저 `updateSound`를 호출하는 동작에 맞춰 WebAudio mute를 제거하고, PAUSING 중에도 PSG 스트림과 레지스터 진행을 유지한다.
- 점멸 위상, 게임/지도 정지, 트리거 프레임 로직과 mute 미호출을 `build/asset-check-pause.txt`로 검증하고 `build/browser-stage5-pausing.png`에서 하단 우측 위치를 확인했다.

### 2026-08-01 — 주인공 벽·천장·낙하 충돌 감사

- 원본 `chkChocaAndar`가 Y와 Y+8의 두 셀을 검사하는 구조를 적용해 하체만 보던 기존 구현에서 상체가 벽을 통과하는 문제를 제거했다.
- 0.65625px 고정소수점 보행에서 실제 이동량이 0인 프레임에도 충돌 시 X를 1픽셀 반대로 빼던 오류를 수정했다. 이제 발생한 정수 이동량만 정확히 되돌린다.
- `chkCae`처럼 바닥 접점을 잃는 최초 프레임에 X 하위 3비트를 기준으로 좌우 4픽셀 경계에 정렬한 뒤 4px/frame 낙하를 시작한다.
- `chkSaltar`, `chkChocaTecho`, `chkChocaSalto`의 머리 두 점, 방향 앞쪽 상·중·하 공간과 수평 점프 벽을 검사하고, 충돌 시 Y를 8픽셀 경계로 맞춰 점프를 종료한다.
- SPACE 점프 트리거 프레임에는 원본 상태 분기처럼 보행 이동을 함께 실행하지 않으며, 점프 자세와 착지 자세를 즉시 설정한다.
- 0픽셀 보행, 상체 벽, 낙하 X 보정, 낮은 천장, 수평 점프 벽 회귀와 15개 스테이지별 300프레임 상태 스모크를 `build/asset-check-collision-audit.txt`로 검증했다.

### 2026-08-01 — 대표 화면 framebuffer hash 회귀

- 가짜 Canvas 2D context 위에서 실제 `Screen`의 256×192 RGBA 버퍼와 SAT 합성을 실행하는 Node 렌더 테스트 하네스를 `tools/check_kingsvalley_assets.mjs`에 통합했다.
- 완성 타이틀, stage 1 활성 화면, 피라미드 지도, SPECIAL BONUS 엔딩, stage 5 GAME OVER의 전체 49,152픽셀을 SHA-256으로 고정했다.
- 타일·색상·스프라이트·HUD·좌표·그리기 순서 중 한 픽셀이라도 바뀌면 회귀 검사가 실패하며 결과는 `build/asset-check-frame-hashes.txt`에 기록한다.
- 이 단계에서는 사용자 원본 캡처와 브라우저 캡처의 수동 비교 및 ROM 데이터 기반 구조 검증을 완료했다. 이후 openMSX 21.0 자동 캡처와 직접 픽셀 diff까지 추가 완료했다.

### 2026-08-01 — 퇴장 문 SAT 4스프라이트 가림

- 퇴장 중 문의 우측 2열을 배경 타일로 주인공 뒤에 다시 합성하던 임시 구현을 제거했다.
- 원본 `attribDoor`대로 `D8`/색1, `DC`/색3을 위쪽 같은 좌표에, `E0`/색1, `E4`/색3을 16픽셀 아래 같은 좌표에 배치한다.
- 문 중심에서 X+16, 화면상 Y−16을 사용하고 문 스프라이트를 SAT 슬롯 0–3에 먼저 등록한 뒤 주인공 의상·피부를 슬롯 4–5에 등록한다.
- MSX 한 라인 4스프라이트 제한과 낮은 슬롯 우선순위에 의해 주인공이 문의 우측 잎 뒤로 가려지는 실제 합성 순서를 복원했다.
- 6개 SAT 슬롯 수, 4개 패턴 바이트, 위·아래 좌표를 `build/asset-check-door-sat.txt`로 검증하고 `build/browser-stage1-exit-sat-overlap.png`에서 퇴장 겹침을 확인했다.

### 2026-08-01 — 실제 pattern ID 이름 테이블 미러

- 기존 `Screen.mirrorTile`은 화면 셀마다 임시 ID를 만들고 패턴 바이트를 복제해 픽셀만 맞췄기 때문에 VRAM name table 자체는 원본과 달랐다.
- `vramTile`에 선택적 `patternId`를 추가하고 메뉴 글꼴·로고, 게임 타일, 지도, 엔딩의 모든 ROM 기반 호출에서 실제 8비트 패턴 번호를 전달한다.
- name table에는 실제 ID를 쓰고 해당 화면 64픽셀 bank의 pattern generator/color table에서 같은 ID 위치에 8바이트를 기록해 SCREEN 2 미러 구조를 복원했다.
- 검은 커튼과 GAME OVER 12×5 삭제 영역도 픽셀 사각형이 아니라 패턴 0의 이름 테이블 쓰기로 전환했다.
- 메뉴 `0x9B/0x9C`, 지도 전체 768바이트, GAME OVER 삭제·문자 ID를 `build/asset-check-vram-name-table.txt`로 검증했고 대표 화면 framebuffer hash 5종이 변하지 않음을 확인했다.

### 2026-08-01 — 결정론적 AY PCM·WAV 파형 회귀

- AY clock 1,789,772.5Hz, 16단계 측정 DAC, 채널별 tone, 17비트 LFSR noise와 tone/noise AND mixer를 샘플 단위로 계산하는 `AyPcmRenderer`를 추가했다.
- 원본 레지스터 쓰기는 즉시 적용되므로 WebAudio의 주파수 5ms·볼륨 10ms 완화 보간을 제거하고 `setValueAtTime`으로 변경했다.
- `tools/render_kingsvalley_audio.mjs`에서 음악 ID와 프레임 수를 받아 44.1kHz mono 16-bit WAV를 생성한다.
- 시작 `0x97` 260프레임 191,355샘플, 게임 `0x8B` 600프레임 441,588샘플, 곡괭이 `0x45` 90프레임 66,238샘플을 각각 `build/kingsvalley-start.wav`, `build/kingsvalley-ingame.wav`, `build/kingsvalley-pickaxe.wav`로 생성했다.
- 세 WAV의 전체 SHA-256을 `tools/check_kingsvalley_assets.mjs`에 고정하고 `build/asset-check-audio-waveforms.txt`에서 통과했다. 이후 openMSX 실제 혼합 WAV와의 자동 파형 비교도 추가 완료했다.

### 2026-08-01 — 전체 통합 회귀 완료

- `sjasm.exe`로 원본 어셈블리를 오류 없이 다시 빌드하고 `RC-727.rom`에서 브라우저 자산을 재생성했다. ROM SHA-256은 `01bd3179540c886ab658626ca0460c329d167327fabd43eb615883375e9e9a3e`로 유지된다.
- 전체 JavaScript 구문 검사, 15개 스테이지 구조·게임플레이 회귀, 대표 화면 5종 framebuffer hash, AY 음악·효과음 WAV hash를 한 번에 실행해 모두 통과했다.
- 이 시점의 통합 결과는 `build/asset-check-final-20260801-all.txt`에 기록했다. 이후 openMSX 실행기를 준비해 화면·VRAM·SAT·PSG·WAV 직접 비교까지 완료했다.

### 2026-08-01 — openMSX 원본 화면·VRAM·SAT 직접 비교

- 공식 openMSX 21.0 포터블 실행기를 `build/tooling/openmsx-21.0`에 준비하고 `tools/capture_kingsvalley_openmsx.mjs`에서 C-BIOS MSX1과 `RC-727.rom`을 자동 실행한다.
- 고정 시간 대기 대신 원본 `WaitMainMenu=0x410D`, `setupStage=0x6AA3`, `gameLogic=0x41D5` breakpoint를 사용해 완성 타이틀과 무입력 60프레임 stage 1–15를 캡처한다. 각 시점의 320×240 raw PNG, VRAM 16KB와 CPU 메모리 64KB를 함께 저장한다.
- openMSX raw PNG의 32×24 border를 제거하고, 원본 VRAM의 이름·패턴·색 테이블과 `0x3B00` SAT를 SCREEN 2 규칙 및 라인당 4스프라이트 제한으로 재렌더한다.
- 직접 비교에서 타이틀 `G` 하단 `C7/C8` 이름 타일과 `0x638–0x647` 색 `0x40` 채우기 누락을 찾아 복원했다.
- `ChgStoneColor`가 같은 스트림을 패턴 `0x40–0x44` 다섯 개와 3개 bank에 반복하는 루프를 포트가 한 패턴만 처리하던 오류를 수정했다.
- 정지 주인공을 원본 `protaFrame=1` 자세로 유지하고, 등장 중 미라의 짝·홀 frame Y bob 공식을 원본 SAT 계산으로 교정했다.
- 최종적으로 타이틀과 stage 1–15의 이름·패턴·색 바이트 불일치가 모두 0이며, 배경과 SAT 합성 후 각 256×192 전체 49,152픽셀 불일치도 모두 0이다. 결과는 `build/asset-check-openmsx-direct.txt`와 `build/openmsx-reference/comparison.json`에 기록한다.

### 2026-08-01 — openMSX PSG·WAV 직접 비교

- `setMusic=0x7AA9`에서 A가 `0x97`인 breakpoint를 잡아 시작 음악의 openMSX PSG 16바이트 snapshot을 260프레임 기록하고 실제 혼합 출력을 `start-openmsx.wav`로 동시에 저장한다.
- PSG 오디오 레지스터 0–13의 3,640바이트를 JS와 비교해 불일치 0, 양쪽 SHA-256 `dc392d80666c3e1bd817306c8fec3c9f0905b213a048de64b39a4cdb10954877`을 확인했다.
- 비교 과정에서 음표 상위 nibble `0xC`가 볼륨 0이면서도 `freqNotas[12]`, 즉 바로 뒤 `MusicIndex` 첫 바이트 `0x39`를 읽어 octave·chorus 주기를 쓰는 ROM 경계 동작을 복원했다.
- openMSX WAV와 결정론적 AY PCM을 한 프레임 정렬한 RMS envelope 상관은 0.919739이며, `CHANNEL_GAIN=0.143` 보정 뒤 평균 RMS 비는 1.001483이다.
- PSG exact 비교와 WAV 상관·음량 임계값은 `tools/compare_kingsvalley_psg_openmsx.mjs`에서 자동 실패 조건으로 고정하고 `build/asset-check-openmsx-psg.txt`에 기록한다.

### 2026-08-01 — 원본 직접 비교 포함 최종 통합 완료

- 원본 어셈블리 재빌드, ROM 자산 재생성, 전체 JavaScript 구문 검사와 15개 스테이지 게임플레이 회귀를 다시 실행해 모두 통과했다.
- 대표 화면 5종 framebuffer golden hash와 시작·게임·곡괭이 음원 WAV golden hash를 최종 값으로 갱신하고 모두 통과했다.
- openMSX 타이틀·stage 1–15의 이름·패턴·색 테이블과 SAT 합성 framebuffer는 각 화면 49,152픽셀 불일치 0이다.
- 시작 음악 260프레임의 PSG 레지스터 3,640바이트는 불일치 0이며, 실제 openMSX WAV와 브라우저 AY 출력의 envelope 상관 0.919739와 평균 RMS 비 1.001483을 확인했다.
- 최종 결과는 `build/asset-check-final-20260801-all.txt`, `build/asset-check-openmsx-direct.txt`, `build/asset-check-openmsx-psg.txt`에 기록했다. 현재 계획에 남은 구현·검증 항목은 없다.

### 2026-08-01 — openMSX 전체 15개 피라미드 직접 비교

- 캡처 범위를 stage 1에서 stage 1–15로 확장하고, 각 스테이지의 ROM 문 데이터에서 기본 입구 슬롯 방향을 계산해 `piramideDest`와 `puertaEntrada`에 주입한다.
- 새 게임 초기화가 목적 스테이지를 1로 덮어쓰므로 메뉴가 아니라 원본 `setupStage=0x6AA3` breakpoint 직전에 값을 설정하도록 캡처 시점을 교정했다.
- stage 2 대조에서 배경·좌표는 일치하지만 주인공 2색 스프라이트 32픽셀만 반전된 것을 발견했다. 원본 입장 직후 자세처럼 모든 기본 입구에서 왼쪽을 바라보도록 초기 방향을 수정했다.
- 타이틀과 stage 1–15 전체에서 visible name/pattern/color VRAM 불일치 0, 배경 framebuffer 불일치 0, SAT 합성 framebuffer 불일치 0을 확인했다.
- `STAGES=2-15`처럼 환경 변수를 지정하면 공통 타이틀·PSG를 건너뛰고 선택한 원본 스테이지만 다시 캡처할 수 있다.

### 2026-08-01 — 2화면 피라미드 비진입 방 직접 비교

- stage 2·4·6·8·10·12·14의 기본 입구 반대편 방 7개를 추가해 15개 피라미드의 총 22개 방을 모두 원본 직접 비교 대상으로 확장했다.
- 첫 `gameLogic` 진입 뒤 60틱의 원본 상태를 유지한 채 `ProtaRoom`만 대상 방으로 바꾸고 `setupRoom`의 `drawRoom`, `updateSprites`, `AI_Cuchillos`까지 실행해 PNG·VRAM·RAM을 저장한다.
- 직접 방 렌더에서는 원본 SAT가 직전 방에서 계산된 값을 유지하므로 비진입 방은 배경 framebuffer와 visible name/pattern/color VRAM을 판정하고, 정상 진입 화면 15개에서는 기존대로 SAT 합성 전체 framebuffer까지 판정한다.
- 타이틀 1개, 진입 화면 15개, 비진입 방 7개를 합친 23개 캡처에서 전체 배경 픽셀 불일치 0, name/pattern/color VRAM 불일치 0, 진입 화면 SAT 합성 픽셀 불일치 0을 확인했다.
- `ROOMS_ONLY=1`과 `STAGES=4,6,8`을 함께 지정하면 기존 진입 화면과 공통 음원 기준을 다시 만들지 않고 선택 스테이지의 비진입 방만 캡처한다.

### 2026-08-01 — 녹화 데모 동적 비교 재개

- 앞선 “현재 계획에 남은 구현·검증 항목은 없다” 기록은 정적 타이틀·방·PSG 범위의 결론이었다. 녹화 데모의 동적 SAT와 장기 상태를 새 범위로 추가했으므로 포팅 전체 완료 상태를 해제했다.
- `tools/capture_kingsvalley_demo_openmsx.mjs`와 `tools/compare_kingsvalley_openmsx.mjs`에 `DEMO_FRAMES=228-266,456,960-1040` 형식의 단일 프레임·범위 선택을 추가하고 캡처 제한 시간을 120초로 늘렸다.
- openMSX에서 228–345, 440–540, 828–890, 960–1040과 1088–1984 장기 기준의 PNG·RAM·VRAM을 추가 캡처했다.
- 주인공 점프 방향 고정, 점프 꼭짓점의 두 개 0 변위, 보행·점프 벽 검사 위치, 아이템 비대칭 충돌 범위와 칼 방출 X 정렬을 원본 루틴대로 보정했다.
- 곡괭이 action counter가 첫 굴착 뒤 `0xF8`을 보존하도록 하고, 데모 점수·생명 처리가 녹화 입력을 중단하지 않도록 분리했다. 그 결과 샘플 960–1984프레임의 동적 배경과 visible name/pattern/color VRAM 불일치는 모두 0이다.
- 미라의 보행 벽 검사, 점프 꼭짓점, 5타일 양방향 계단 탐색, 플레이어 원본 논리 좌표(`화면 좌표-1`), 망설임 카운터가 0이 되는 같은 틱의 결정 전환을 반영했다.
- 미라가 칼에 맞아 림보에서 돌아올 때 원본이 `ACTOR_MOV_CNT`를 지우지 않는 동작을 반영해 재등장 보행 애니메이션 카운터를 보존한다.
- 원본 `tickGame`이 actor 속성을 VRAM에 먼저 복사한 뒤 AI를 실행하는 점을 반영한 별도 SAT snapshot과 주인공 계단의 전역 timer 주입을 각각 시험했다. 두 방식 모두 일부 기준 프레임은 개선했지만 대표 321·464프레임과 장기 맵 진행을 더 크게 악화해 되돌렸으며, 액션별 RAM 속성 갱신 시점을 분리하는 후속 직역 항목으로 남겼다.
- 동적 전체 framebuffer는 일부 프레임에서 완전 일치하지만 고정소수점 보행/SAT 갱신 위상과 960프레임 이후 미라 계단 진입 시점에 잔여 차이가 있다. 기본 비교 목록을 1984프레임까지 확장했으며 이 차이가 남아 있는 동안 비교기는 의도적으로 실패한다.
- 자산 회귀는 변경된 엔딩 점프 길이, 정확한 보석 충돌 범위, 미라 계단 탐색 반경, 투척 22틱, 보행·점프 벽 위상을 반영해 갱신했고 `node tools/check_kingsvalley_assets.mjs`를 통과했다.
- 당시 기본 동적 기준 42프레임에서 배경 41/42, SAT 합성 전체 framebuffer 21/42가 완전 일치했다. `walkCounter=0` 보정 후 최신 기준은 배경 41/42, framebuffer 19/42이며 대표 장기 기준 832/960/1088/1216/1984의 잔여 불일치는 각각 172/166/181/327/337픽셀이다.
- ASM 재빌드, 전체 JavaScript 구문, 자산·게임 상태 회귀, 정적 타이틀·15스테이지·22개 방 직접 비교와 PSG 직접 비교는 모두 통과했다. 결과는 `build/asset-check-final-latest.txt`, `build/asset-check-openmsx-static-latest.txt`, `build/asset-check-openmsx-psg-latest.txt`에 저장했고, 의도적으로 실패하는 동적 잔여 보고서는 `build/asset-check-openmsx-dynamic-latest.txt`와 `build/openmsx-reference/comparison-dynamic-latest.json`에 저장했다.

### 동적 검증 잔여 체크리스트

- [x] 데모 입력 0–1984프레임에서 맵 파괴·아이템 점유·보석 상태가 원본 visible VRAM과 일치한다.
- [x] 321·464프레임 등 대표 동작 프레임에서 SAT 합성 framebuffer 불일치 0을 확인한다.
- [x] 망설임 종료 같은 틱 결정과 재등장 보행 카운터 보존을 원본 RAM 구조체와 대조한다.
- [ ] 주인공 고정소수점 X와 보행 animation frame의 VBlank/SAT 갱신 위상을 모든 기본 데모 프레임에서 일치시킨다.
- [ ] 937프레임 이후 첫 미라의 벽 반전 시점과 1984프레임 장기 경로를 원본과 일치시킨다.
- [ ] 데모 종료 sentinel 뒤 메뉴 복귀까지 캡처·비교하고 동적 framebuffer 불일치 0을 달성한다.

### 2026-08-01 — 미라 CONTROL/SENTIDO 분리 실험

- 원본 미라 구조체의 이동 제어와 표시 방향이 별도 필드라는 점을 반영해 JS에 `moveDirection`을 추가했다. 망설임 중 표시 방향은 유지하면서, 계단·벽·점프·경로 탐색은 이동 방향을 사용한다.
- 방향 통합 실험은 960프레임에서 경로가 다시 어긋났지만, 분리 후 기본 동적 42프레임의 배경 exact 41/42와 framebuffer exact 21/42를 유지하고 대표 장기 불일치를 1088: 263→181, 1216: 351→327, 1984: 341→337픽셀로 줄였다.
- 321·464프레임은 계속 framebuffer 불일치 0이며 정적 15스테이지·22개 방·PSG·자산 회귀도 재실행해 통과했다. 남은 문제는 주인공 고정소수점/애니메이션 위상과 960프레임 이후 첫 미라의 실제 계단 경로다.
- 625–832프레임을 매 틱 추가 캡처해 확인한 결과, 배경은 전 구간 exact이며 주인공의 고정소수점 위치와 `protaMovCnt`/`protaFrame`이 625프레임 전후부터 분리된다. 이 구간은 `build/openmsx-reference/comparison-player-dense.json`으로 보존했다.
- 원본 시작 카운터에 맞춰 플레이어 `walkCounter` 초기값을 1에서 0으로 보정했다. 832프레임 framebuffer 오차가 191→172픽셀로 감소했고, 321·464·960·1088·1216·1984 대표 결과는 유지됐다.
- 주인공 계단 이탈을 한 틱 지연하는 전체·좌측 조건부 실험은 대표 맵 VRAM까지 악화되어 되돌렸다. 계단 상태 전환은 현재 원본 루틴 기반 동작을 유지하고, 캡처 VBlank 위상 보정 항목으로 남긴다.
- 계단 이탈 직후 첫 보행만 건너뛰는 별도 실험도 동일하게 맵 VRAM을 악화시켜 되돌렸다. 현재는 게임플레이 상태를 바꾸지 않는 카운터·스프라이트 위상 보정만 채택한다.
- 미라 보행의 벽 검사 순서를 원본처럼 이동 전으로 바꾸는 실험은 321/464프레임과 625프레임 이후 framebuffer 오차를 각각 120/144/205픽셀로 늘려 되돌렸다. 현재 JS 기준선은 이동 후 검사 모델을 유지하며, 원본 RAM 위상과 JS 상태 갱신을 분리해 추가 조사한다.
- 최신 플레이어 SAT 스냅샷 실험은 832프레임에는 개선이 있었지만 464·625·960·1216프레임을 악화시켜 되돌렸다. 렌더 스냅샷은 전역 적용하지 않고 원본 `updateSprites` 호출 시점에 맞춘 개별 상태 추적 과제로 남긴다.
- 원본 `KeyHold`와 JS 데모 입력의 656/657프레임 전환 차이를 확인해 `DemoReplay` 초기 카운터 7을 시험했지만 321/464 framebuffer와 1088 이후 배경까지 악화되어 되돌렸다. 기존 카운터 8과 ROM 입력 데이터는 유지한다.
- Konami 스플래시 타이틀의 MSX 색상 0을 검정으로 그리던 차이를 수정했다. 원본 `SetVideoMode`의 파란 backdrop을 스플래시 전용 팔레트로 적용하고, 메뉴·게임 화면은 기존 팔레트를 유지한다.
- 스페이스 입력 후 `starting` 상태에서 원본 `TXT_PLAY_START`와 같은 14칸 위치를 사용하도록 바꾸고, `messageTimer` bit2로 4프레임 주기 점멸을 구현했다. `tools/check_kingsvalley_assets.mjs`에 표시·삭제 위치 회귀 검사를 추가했다.
- 원본 `iniciaPartida`의 `waitCounter=0x50`을 반영해 시작 음악 종료를 기다리던 동작을 80프레임 `PLAY START` 점멸 후 32프레임 커튼 전환으로 수정했다. 점멸 위상도 남은 waitCounter의 bit2 기준으로 맞추고 회귀 검사를 통과했다.
- 첫 스테이지 커튼 구간에는 원본처럼 마지막 `PLAY START` 화면을 밑그림으로 유지하고, 자동 데모 커튼은 기존 메뉴 밑그림을 유지하도록 분리했다.
- `tools/check_kingsvalley_assets.mjs`가 실제 `Game.tick()` 80회와 커튼 32회를 실행해 `starting → curtain-to-first-level → play` 전환을 검증하도록 보강했다.
- 미라 림보 갱신을 원본 전역 `timer & 1`로 대체하는 실험은 832/1984프레임 일부를 개선했지만 464·625·960프레임을 악화해 되돌렸다. 현재 로컬 phase 토글 기준선을 유지하고, SAT 시점과 AI 상태를 별도 추적한다.

### 2026-08-01 — 미라 낙하·계단 장기 경로 보정

- openMSX 데모 321–456, 625–832, 833–1088 구간을 매 프레임 추가 캡처해 첫 번째 미라의 22바이트 `momiaDat` 레코드와 JS 상태를 직접 대조했다. 캡처 로그는 `build/capture-demo-321-456.txt`, `build/capture-demo-625-832.txt`, `build/capture-demo-833-1088.txt`에 보존했다.
- 원본 `momiaSaltando → momiaCayendo`와 `momiaSinSuelo → momiaCayendo`는 분기한 같은 틱에 낙하 루틴까지 실행한다. JS도 상태만 바꾸고 한 틱 기다리지 않고 즉시 4픽셀 낙하/착지 검사를 수행하도록 수정했다.
- 원본 `chkCae`는 정수 X만 4픽셀 경계에 맞추고 `ACTOR_XDEC`는 보존한다. JS의 플랫폼 이탈 정렬도 소수부를 버리지 않도록 바꿔 654프레임부터 발생하던 0.75픽셀 누적 오차를 제거했다.
- `momiaAnda`가 생각 상태 7로 전환하는 틱에는 기존 보행 frame을 유지하고, 실제 망설임 timer가 남아 있을 때만 frame 2를 사용하도록 `momiaPiensa` 순서를 복원했다.
- 원본 계단 루틴은 느린 미라가 이동하지 않는 pause 틱에도 `momiaCalcFrame`을 실행하며, 이동 틱에는 `andaEscalera`와 `momiaCalcFrame`이 각각 보행 카운터를 올린다. JS도 pause 틱 +1, 이동 틱 +2로 맞췄다.
- 이동 전 벽 검사와 다음 틱 예약 충돌 모델은 936프레임 국소 상태를 개선했지만 321프레임 이전 경로를 크게 훼손해 모두 되돌렸다. 현재 검증된 낙하·소수 좌표·생각 frame·계단 counter 수정만 유지한다.
- 대표 동적 framebuffer 잔여 불일치는 중간 보정 시점에 `832/960/1088/1216/1984 = 128/147/64/179/351`픽셀까지 감소했다. 이후 벽 반전·SAT·AI를 계속 보정했으며 모든 동적 배경은 계속 0픽셀 불일치다.
- 첫 번째 미라의 937프레임 벽 반전은 충돌 즉시 좌표를 되돌리지 않고 다음 보행 틱에 방향만 반전하는 `wallTurnPending`으로 복원했다. 점프 가능한 벽 충돌은 원본처럼 같은 틱에 처리해 이전 경로를 보존했다.
- `node tools/check_kingsvalley_assets.mjs`, `STAGES=1-15` 정적 openMSX 직접 비교, JavaScript 구문 검사와 `git diff --check`를 다시 통과했다.
- 0–320프레임도 매 틱 캡처해 `build/capture-demo-0000-0320.txt`에 추가했다. 원본 림보 토글과 맞추기 위해 초기 `phaseTick`을 반전한 실험은 832/1088/1216 오차를 179/76/213픽셀로 악화해 되돌렸다.
- 이동 후 처음 충돌 경계에 닿은 경우만 다음 틱으로 예약하는 제한 실험도 321/464/625의 기존 완전·근접 일치를 깨뜨려 되돌렸다. 벽 반전은 현재 AI 좌표를 변경하지 않고 SAT/RAM 갱신 시점을 분리하는 방식으로 계속 추적한다.
- 원본 VDP SAT가 미라 AI보다 앞선 상태를 표시하는 순서를 반영해, 활성 미라별 이전 좌표·frame·방향을 `renderState`에 저장하고 렌더링에만 사용하도록 분리했다. 이 보정으로 1088프레임 전체 framebuffer가 0픽셀 불일치가 됐다.
- openMSX `guardaOrden(0x73CC)` 중단점으로 1208프레임 주변 검색을 추적했다. 흰 미라(type 0)는 아래 타일이 빈 첫 후보에서 방향 검색을 즉시 끝내지만 다른 타입은 계속 검색하므로, 낙하 중단 규칙을 type 0에만 적용했다.
- 새 계단 후보가 없을 때 기존 상·하행 명령을 지우지 않도록 고쳤다. 원본 `setOrden*`은 주문 슬롯이 0이면 기존 `ACTOR_CONTROL`을 보존하며, 이 차이를 복원해 1389프레임 계단 진입 경로를 맞췄다.
- AI의 플레이어 세로 비교는 `player.y`를 사용하고, 플레이어가 계단 이동 중일 때만 원본 `ProtaY` 갱신 차이 2픽셀을 더한다. 1556프레임의 잘못된 좌회전과 1914프레임의 누락된 `아래+오른쪽(0x0A)` 명령을 함께 복원했다.
- 계단을 향하는 수평 방향은 명령을 결정할 때 한 번만 정한다. 매 보행 틱마다 목표 X와 재비교해 1716프레임 부근에서 좌우로 진동하던 동작을 제거했다.
- 플레이어와 접촉하면 미라 갱신 루프를 `break`하던 JS 전용 동작을 제거했다. 원본은 충돌 처리와 `AI_Momias` 전체 순회를 분리하므로, 피격을 기록하되 남은 미라를 계속 갱신한다. 이 보정으로 1749–1856 구간의 두 번째 미라 정지가 사라졌다.
- 매 프레임 원본 캡처를 `1089–1216`, `1217–1344`, `1345–1472`, `1473–1600`, `1601–1728`, `1729–1856`, `1857–1984`까지 확장했다. 캡처 목록은 각 `build/capture-demo-*.txt`에 기록했다.
- 최신 직접 RAM 대조에서 두 미라의 status·X 소수 좌표·Y·movement counter·frame·stress가 1914–1984 전 프레임 일치한다. 1088프레임 framebuffer도 완전 일치한다.
- 플레이어의 원본 이동 카운터와 JS 시각 카운터 사이 9단계 위상을 물리와 분리해 렌더 frame에 적용했다. 하강 계단은 같은 틱의 다음 `x±1, y+1` 좌표를 사용하고, 자유낙하는 원본 `protaCayendo`처럼 frame 4와 SAT Y -1을 유지한다.
- 위 플레이어 SAT 보정 후 최신 대표 잔여치는 `1472/1600/1715/1728/1856/1914/1984 = 0/0/0/0/36/0/0`픽셀이다. 1856은 우측 보행 플레이어 X 1픽셀만 남고, 해당 구간의 미라와 배경은 완전 일치한다.
- 장기 대표 기준 `625/832/960/1216/1344 = 74/92/108/87/87`픽셀은 플레이어 위치 위상 차이만 남는다. 우측 보행에 직전 0xA8 이동값을 전역 적용하는 실험은 이 이전 구간을 악화시켜 되돌렸으며, 입력 구간별 플레이어 논리 좌표를 먼저 복원하는 과제로 유지한다.
- 데모 전체 입력 카운터를 1프레임 앞당기는 실험과 새 SPACE 트리거만 앞당기는 실험은 이전 점프의 착지 플랫폼을 바꿔 장기 경로를 훼손하므로 되돌렸다. 입력 스트림은 유지하고 플레이어 상태/SAT 갱신 순서를 직접 맞춘다.
- 칼 보유 중 SPACE 트리거만 레코드 경계에서 앞당기는 실험도 기록했다. 원본 status 시점은 맞았지만 321프레임 칼 애니메이션 SAT가 111픽셀 악화되어 되돌렸고, 칼의 액션 frame 순서 자체를 별도 보정 대상으로 남겼다.
- 플레이어 계단 이동 parity를 원본 `andaEscalera`의 전역 timer parity로 직접 치환하는 실험도 수행했다. 321프레임부터 계단 경로가 달라져 대표 오차가 700픽셀대로 증가했으므로 되돌렸고, 현재 로컬 movement timer와 원본 timer의 위상 매핑을 별도로 추적한다.

### 2026-08-01 — 미완성 기능 우선순위 전환

원본과 프레임 단위 framebuffer 일치 작업은 현재 동작 가능한 기준선을 유지하되 후순위로 내리고, 실제로 게임을 끝까지 플레이할 수 있는 기능을 먼저 완성·회귀 검증한다.

- [x] P0 `stage 1 → stage 15 → ending`의 실제 보석 수집·출구 출발·문 폐쇄·보너스·맵·다음 스테이지 통합 흐름을 자동 회귀로 고정했다. `tools/check_kingsvalley_gameplay.mjs`가 15개 출구 전이와 사망 후 재시작까지 실행한다.
- [x] P1 아이템 lifecycle을 보강했다: 칼 획득 → 투척 → 적중/튕김/낙하 → 재획득, 곡괭이 바닥·측면 굴착과 구멍 복구를 개별 검사와 실제 `Level.update()` 입력 경로에서 회귀한다.
- [x] P1 상호작용 예외를 보강했다: 계단·회전문 통과, 함정 낙하·압사, 적 피격→생명 감소→재시작, 방 스크롤 경계를 회귀한다.
- [x] P2 브라우저 입력·AudioContext 시작·일시정지·게임오버·데모 종료 상태 전이를 회귀 검증했다. Space edge·Z 무시·blur 초기화와 데모 sentinel 2000프레임 후 스플래시 복귀를 `tools/check_kingsvalley_gameplay.mjs`에서 고정했고, PSG/WAV golden·일시정지·게임오버 검사는 기존 자산 회귀에 유지한다.
- [ ] P3 위 기능이 안정화된 뒤에만 `kvalley.lst`의 VBlank/SAT 위상과 주인공·미라의 잔여 framebuffer 차이를 다시 줄인다.

현재 정적 자산·15개 스테이지 배경·22개 방·PSG·개별 플레이어/아이템/충돌 검사와 P0–P2 통합 흐름이 통과했다. 다음 작업은 기능 기준선을 보존한 채 P3 프레임 정밀도 잔여분을 선택적으로 다루는 것이다.

P0–P2 기능 우선순위 작업은 완료했다. 남은 P3는 기능을 변경하지 않는 범위에서만 `kvalley.lst`의 VBlank/SAT 위상과 주인공·미라의 잔여 framebuffer 차이를 줄이는 후순위 작업이다.

## 검증 명령

```powershell
tools\bin\sjasm.exe -s src\kvalley.asm build\kvalley.rom build\kvalley.lst build\kvalley.sym
node tools\export_kingsvalley_assets.mjs rom\RC-727.rom build\kvalley.sym web\src\game\romdata.js
node tools\check_kingsvalley_assets.mjs
node tools\check_kingsvalley_gameplay.mjs
node tools\trace_kingsvalley_psg.mjs 0x97 260 build\psg-trace-start.json
node tools\render_kingsvalley_audio.mjs 0x97 260 build\kingsvalley-start.wav
node tools\capture_kingsvalley_openmsx.mjs
node tools\compare_kingsvalley_openmsx.mjs
node tools\compare_kingsvalley_psg_openmsx.mjs
node tools\capture_kingsvalley_psg_events_openmsx.mjs
node tools\compare_kingsvalley_psg_events_openmsx.mjs
node tools\check_kingsvalley_audio_webapi.mjs
python -m http.server 8000 --directory web
```
### 2026-08-01 — `kvalley.lst` 기준 기능 포팅 현황

`src/kvalley.lst`의 주요 실행 루틴을 JS 기능 단위로 재분류했다. 아래 상태는 정적 화면만 맞춘 비율이 아니라, 실제 JS 게임 루프와 회귀 검증에 포함됐는지를 기준으로 한다.

| `kvalley.lst` 기능 블록 | JS 포팅 범위 | 상태 |
|---|---|---:|
| `tickGame`·`updateSprites`·게임 모드 전환 | `web/src/game/flow.js`, `level.js`, `entity.js` | 기능 완료 |
| `AI_Prota`·플레이어 이동/점프/계단 | `web/src/game/player.js` | 기능 완료 |
| `AI_Cuchillos`·칼 투척/적중/튕김/낙하/재획득 | `web/src/game/entity.js`, `level.js` | 기능 완료 |
| `AI_Gemas`·보석 충돌/반짝임/맵 제거 | `web/src/game/level.js` | 기능 완료 |
| `AI_Salidas`·출구 개방/출발/문 폐쇄/클리어 | `web/src/game/level.js`, `flow.js` | 기능 완료 |
| `MurosTrampa`·함정 낙하/정지/압사 | `web/src/game/level.js` | 기능 완료 |
| `spiningDoors`·회전문 밀기/통과/맵 갱신 | `web/src/game/level.js` | 기능 완료 |
| `setupStage`·`setupRoom`·맵/방/스크롤 | `web/src/game/data.js`, `level.js` | 기능 완료 |
| `AI_Momias`·미라 보행/점프/낙하/계단/림보 | `web/src/game/entity.js` | 기능 완료, 동적 SAT 잔여 |
| Konami 스플래시·타이틀·메뉴·`PLAY START` | `web/src/game/flow.js`, `rom.js` | 기능/타이밍 회귀 완료 |
| `intro`·지도·HUD·커튼·엔딩·게임오버 | `web/src/game/flow.js`, `web/src/game/level.js`, `web/src/game/state.js` | 기능 완료·회차 HUD 보완 |
| PSG 음악·효과음·곡괭이 음원 | `web/src/psg.js`, `sound.js`, `audio.js` | 원본 이벤트 240프레임 직접 일치 |
| 15개 스테이지 실제 진행·엔딩·재시작 | `tools/check_kingsvalley_gameplay.mjs` | 통합 회귀 완료 |

현재 `tools/check_kingsvalley_assets.mjs`와 `tools/check_kingsvalley_gameplay.mjs`가 정적 자산, 입력, 아이템, 충돌, 15개 스테이지 전이, 엔딩, 데모 종료를 통과한다. Konami 스플래시는 `kvalley.lst`의 14단계·2프레임 상승·255프레임 유지로 보정했고, 메뉴 자동 데모 대기는 256프레임으로 보정했으며, `PLAY START` 80프레임 점멸과 32프레임 커튼 회귀도 통과한다.

### 2026-08-01 — Konami/타이틀/메뉴/PLAY START 우선 보완

- 원본 `KonamiLogo`의 14단계 상승을 실제 2프레임 주기로 맞추고, 최종 로고·밑줄·`SOFTWARE` 위치를 회귀 검증했다.
- 원본 `waitCounter`의 로고 유지 구간 0xFF와 메뉴 진입 후 0x100 무입력 대기를 반영해 자동 데모가 너무 빨리 시작되던 차이를 수정했다.
- `PLAY START`의 원본 0x50 점멸 대기, 4프레임 blink 위상, 첫 커튼 32프레임 전환은 기존 동작을 유지하고 회귀 검증했다.
- `tools/check_kingsvalley_assets.mjs`에 스플래시 1·2·3·27프레임 위치, 255프레임 유지, 메뉴 자동 데모 256프레임 검사를 추가했다.
- 이 우선 작업 결과 `Konami/타이틀/메뉴/PLAY START`는 기능·타이밍 기준 완료로 갱신했으며, 남은 P3는 게임 중 주인공·미라 동적 SAT 위상이다.

### 2026-08-01 — PSG 음악·효과음 잔여개발

- 원본 `updateSfxCaer`가 사용하는 `E03B–E03E` RAM 동적 버퍼를 `web/src/psg.js`에 복원했다. 낙하음은 더미 `FF`로 즉시 종료되지 않고, 초기 주파수 `0x061`에서 매 프레임 `+8` 및 carry를 반영한다.
- raw PSG의 bit6 이벤트는 원본처럼 토큰 하위 4비트를 볼륨으로 사용하도록 수정했다. 이 차이로 곡괭이음 `0x45`가 작게 들리던 문제가 해결됐다.
- `tools/capture_kingsvalley_psg_events_openmsx.mjs`와 `tools/compare_kingsvalley_psg_events_openmsx.mjs`를 추가해 낙하·회전문·획득·곡괭이·투척·미라·피격·보석·생명·게임곡·문·지도·클리어·시작·게임오버 이벤트 16종을 원본에서 직접 캡처한다.
- 16개 이벤트의 260프레임, PSG 14개 오디오 레지스터를 openMSX와 비교해 전부 불일치 0을 확인했다. 시작곡은 기존 260프레임 레지스터 불일치 0, WAV envelope 상관 `0.919739`, RMS 비 `1.001483`도 유지한다.
- 곡괭이음 golden hash를 수정된 원본 토큰 해석 결과 `2c35411fa481e7cea452260f8bd6773a339001116d9fffe87e04fc431dd6ae60`으로 갱신하고 자산·게임플레이 회귀를 다시 통과시켰다.
- `tools/check_kingsvalley_gameplay.mjs`에 실제 통합 진행 중 발생하는 PSG 호출 로그를 추가했다. 15개 스테이지 전이, 아이템 사용, 보석 완성, 피격, 게임오버에서 호출되는 이벤트 ID를 한 번에 확인한다.

#### PSG 남은 작업

- [x] 메뉴 상태를 `GameStatus=1`, `waitCounter=FF`로 고정하는 독립 호출 경로를 추가해 16개 이벤트의 260프레임 종료·무한 루프 구간까지 직접 비교했다.
- [x] `tools/check_kingsvalley_audio_webapi.mjs`에서 실제 `Sound.attach` 경로의 AudioContext 생성·resume·PSG 노드 출력·mute·stop을 자동 검증했다.
- [ ] 실제 브라우저별 WebAudio 출력 레벨·noise 음색을 수동 확인하고, 필요하면 `PsgAudio`의 브라우저별 gain 보정을 조정한다. 현재 환경에는 실행 가능한 브라우저 바이너리가 없어 자동 WebAudio API 검증으로 대체했다.
- [x] 실제 플레이 통합 경로에서 효과음 호출 순서와 우선순위를 장기 회귀에 추가했다. 현재 회귀 출력은 148개 호출과 14개 고유 이벤트 ID를 확인한다.

### 2026-08-01 — HUD·커튼·지도·엔딩 잔여개발

`kvalley.lst`의 `renderHUD`, `drawCortinilla`, `tickPergamino`, `setupEnding`을 다시 대조해 기능 기준의 약 90% 잔여분을 마무리했다.

- [x] HUD의 `SCORE/HI/REST` 위치·6자리 점수·2자리 생명 표시를 유지하고, 원본 `numFinishGame * 15 + piramideActual` 계산을 `completedRuns`와 `pyramidDisplayNumber()`로 복원했다. 한 번 엔딩을 본 뒤 다음 게임은 `PYRAMID-16`부터 표시된다.
- [x] 새 게임과 자동 데모 시작 시 회차·스테이지 표시 상태를 원본처럼 초기화했다. 엔딩에서 GOAL 대기 완료 시 회차를 1 증가시키고, 재시작 후 HUD에 반영한다.
- [x] 커튼의 좌→우 32열(0x20 프레임) 지우기 경계를 고정했다. 첫 프레임은 0열, 마지막 프레임은 32열 전체가 검게 되며 첫 레벨·데모·지도·재시작·엔딩 전환에서 공통 사용한다.
- [x] 지도 0x58 목적지 전환, 0xE0 종료, 최종 GOAL 화살표, 8색 점멸, 방향별 화살표 좌표를 회귀한다.
- [x] 엔딩의 32프레임 커튼→0x8B 음악→보행·점프→10000점 보너스/0x8A→0xD0 대기→재시작 커튼/무음 순서를 회귀한다.
- [x] `tools/check_kingsvalley_assets.mjs`에 회차 HUD 번호와 커튼 경계 검사를 추가했다. 정적 화면 hash와 15개 스테이지 통합 흐름은 기존 기준선을 유지한다.

#### HUD·커튼·지도·엔딩 남은 작업

- [ ] 실제 브라우저 캡처로 MSX 에뮬레이터와 커튼 중간 프레임, 지도 점멸 위상, 엔딩 보너스 문구의 표시 위상을 최종 확인한다. 현재 환경에는 실행 가능한 브라우저 바이너리가 없다.
- [ ] `kvalley.lst`의 VBlank 시점과 JS `requestAnimationFrame` 경계를 결합해 HUD·지도·엔딩의 프레임 단위 framebuffer exact를 별도 캡처한다.
- [ ] 엔딩 이후 `numFinishGame`에 따라 미라 타입·난이도가 증가하는 원본 로직을 장기 2회차 플레이 회귀로 확장한다. HUD 번호 자체는 이번 작업에서 완료했다.

### 2026-08-01 — 플레이어·아이템·스테이지 상호작용 순차 보완

사용자가 지정한 우선순위인 `플레이어 이동·점프·계단` → `칼·곡괭이·구멍·아이템` → `보석·출구·회전문·함정` 순서로 `kvalley.lst`의 기능 잔여분을 반영했다.

- [x] **플레이어 이동·점프·계단**: 원본 고정소수점 이동량 `0xA8`, 계단 1픽셀/2프레임 이동, 계단 진입·이탈 좌표, 낙하 정렬, 상단 `ProtaY=0` 점프 금지, 낙하·착지 PSG 이벤트를 검증했다.
- [x] **칼·곡괭이·구멍·아이템**: 칼 준비 카운터 `0x15`, 4개 동시 제한, 벽 앞 직접 반동, 적중 후 반동·낙하·바닥 재배치, 좌측 곡괭이 목표 타일/주인공 정렬, lateral/floor hole timing과 아이템 맵 복원을 반영했다.
- [x] **보석·출구·회전문·함정**: 보석 4셀 맵 제거·점멸, 모든 보석 획득 시 `0x94`와 회전문 맵 제거, 출구 32프레임 애니메이션/레버/출발, 함정 천장 탐색·4픽셀 낙하·오브젝트 대기·압사를 검증했다.
- [x] `tools/check_kingsvalley_assets.mjs`에 좌측 곡괭이 보정, 벽 반동, 칼 제한, 마지막 보석의 회전문 제거 회귀를 추가했다.
- [x] `tools/check_kingsvalley_gameplay.mjs`에서 15개 피라미드 전이·출구·PSG·재시작 통합 회귀를 다시 통과했다.

#### 순차 작업 후 남은 작업

- [ ] 원본 VBlank 직전 SAT와 JS 렌더 순서까지 맞추는 장기 동적 framebuffer 비교. 현재 대표 잔여 차이는 `build/asset-check-openmsx-dynamic-latest.txt`에 기록된 주인공·미라 animation 위상이다.
- [ ] 실제 브라우저에서 계단·구멍 진입, 칼 반동/재획득, 마지막 보석 후 회전문 제거, 함정 압사 입력 녹화를 추가 확인한다. 자동 회귀에는 반영했지만 실행 가능한 브라우저 바이너리가 없어 수동 캡처는 보류 중이다.
- [ ] 2회차 `numFinishGame`에 따른 미라 난이도·타입 증가와 각 상호작용의 framebuffer exact를 장기 회귀에 연결한다.

### 2026-08-01 — Konami 스플래시 SOFTWARE 중앙 정렬 보정

- [x] 원본 `TXT_Sofware`의 8글자 문구를 256픽셀 화면 중앙인 x=96부터 출력하도록 수정했다. 기존 x=128은 문구 폭 64픽셀만큼 오른쪽으로 치우치는 원인이었다.
- [x] `tools/check_kingsvalley_assets.mjs`에 `SOFTWARE` 첫 글자·마지막 글자·우측 빈 칸의 name-table 좌표 회귀를 추가했다.

### 2026-08-01 — 스테이지 클리어 지도 화면 보정

- [x] 원본 지도 화면처럼 `SCORE/HI/REST` HUD를 지도 렌더링에도 출력하도록 복원했다. 기존에는 지도 이름 테이블만 그려 상단 HUD가 비어 있었다.
- [x] 지도 본문 빈 타일(`pattern 0x01`)의 색상 속성을 검정 바탕에서 검정 경로·흰 용지(`0x0F`)로 보정했다. 이로써 원본의 흰 지도 실루엣과 검정 연결선이 분리되어 표시된다.
- [x] 지도 framebuffer 기준선을 `abc905da8ca6bc281eef90792ab03befdd20931c17042bafa2e5233cb6d8e52c`로 갱신하고 HUD name-table·흰 용지 픽셀 미러 회귀를 추가했다.
- [x] 자산 회귀, 15개 스테이지 게임플레이 회귀, JavaScript 문법 검사, `git diff --check`를 통과했다.

#### 지도 화면 남은 작업

- [ ] 실제 브라우저 캡처에서 원본 에뮬레이터와 지도 HUD 수치, 목적지 화살표 점멸 위상, 커튼 중간 프레임을 최종 대조한다.
- [ ] `kvalley.lst` VBlank와 `requestAnimationFrame` 경계 차이를 줄여 지도 전환 framebuffer를 장기 캡처로 비교한다.

### 2026-08-01 — 미라 AI·점프·낙하·계단 데모 기준 90% 보강

- [x] 기존 `AI_Momias` 구현의 타입별 점프 가능 여부, 플레이어 상대 높이, 벽 충돌·반전, 림보·등장, 계단 진입·이탈, 4픽셀 낙하를 `kvalley.lst` 루틴과 재대조했다.
- [x] 점프 변위표 `4,2,2,2,1,1,2,0,1,1,0,0`의 상승·하강 위상과 점프 중 수평 1픽셀 이동을 프레임 단위 회귀로 고정했다.
- [x] 낙하 시작 틱의 4픽셀 정렬, 다음 틱 착지 판정, 계단 타입 0의 pause mask `3` 및 이동 틱의 X/Y/frame 갱신을 별도 회귀로 추가했다.
- [x] 원본 `buscaCercanias`를 직접 치환하는 계단 검색 실험은 openMSX 데모에서 맵 경로를 악화시켜 되돌렸다. 기존 데모 기준선과 검증된 경로를 유지한다.
- [x] 자산 회귀, 15개 스테이지 게임플레이 통합 회귀, JavaScript 문법 검사, `git diff --check`를 통과했다.

#### 미라 AI·점프·낙하·계단 남은 작업

- [ ] openMSX 데모 625프레임 이후 남는 주인공·미라 SAT 표시 위상을 상태 갱신과 분리해 줄인다. 현재 대표 차이는 `build/asset-check-openmsx-dynamic-latest.txt`에 보존한다.
- [ ] 실제 브라우저에서 미라가 벽·계단·플랫폼 경계에서 전환되는 장기 입력 캡처를 추가한다. 현재 환경에는 실행 가능한 브라우저 바이너리가 없어 Node/openMSX 회귀로 대체했다.

### 2026-08-01 — 미라 상태 레코드·SAT 차이 진단 보강

- [x] JS 미라에 원본 `ACTOR_JUMPSENT`, `ACTOR_SENT_ESC`, `ACTOR_POS_RELAT`에 대응하는 지속 상태를 추가했다. 점프 착지나 계단 이탈 뒤에도 원본처럼 마지막 값을 보존한다.
- [x] `tools/compare_kingsvalley_openmsx.mjs`가 각 미라의 status/Y/X 소수부/X/movement counter/frame/jump·stair 상태/timer/type/stress를 원본 memory와 함께 보고하도록 확장했다.
- [x] 진단 결과 840·848프레임은 미라 상태 mismatch 0으로 확인했다. 1088프레임 이후 남은 미라 좌표·stress 차이는 플레이어 장기 이동 위상과 함께 발생하는 별도 P3 항목으로 분리했다.
- [x] 자산 framebuffer·오디오·15개 스테이지 회귀와 JavaScript 문법 검사를 다시 통과했다.

#### 상태 진단 후 남은 작업

- [ ] 1088프레임 이후 두 번째 미라의 실제 `momiaDecide` 입력(플레이어 Y/room/X)과 JS 플레이어 상태를 같은 memory 시점으로 맞춘다.
- [ ] 미라 SAT sprite 슬롯 순서와 플레이어 장기 이동 위상을 함께 보정한 뒤 대표 1216·1344·1600·1984프레임의 framebuffer를 재검증한다.

### 2026-08-01 — stage 5 장기 데모 계단 진입 보정

- [x] 원본 `chkCogeEscalera`의 계단 절반 타일 판정을 재대조해, 플레이어가 인접한 좌측 계단 절반에 걸친 상태에서도 stage 5 계단에 진입하도록 제한적 fallback을 추가했다.
- [x] `tools/compare_kingsvalley_openmsx.mjs`에 원본 `protaStatus/ProtaY/ProtaX/protaFrame/objetoCogido`와 JS 플레이어 상태 비교를 추가해 미라·주인공 차이를 분리 진단한다.
- [x] 전체 데모 기준에서 계단 분기 후 미라 AI mismatch가 대부분 0으로 줄었고, `demo-frame1856` framebuffer는 0픽셀 불일치가 됐다. 1216/1344프레임도 각각 87/81픽셀로 감소했다.
- [x] 넓은 계단 타일 허용과 주인공 이전 프레임 렌더 스냅샷은 전체 합계가 악화되어 폐기하고, 제한적 계단 fallback만 유지했다.

#### 장기 계단 보정 후 남은 작업

- [ ] 주인공의 고정소수점 X와 `protaMovCnt/protaFrame` phase를 액션·계단 전환별로 보정해 8–464프레임 초기 차이와 1216 이후 60–147픽셀 잔여 차이를 줄인다.
- [ ] frame 160의 배경 18픽셀 차이는 칼/아이템 맵 갱신 시점으로 별도 분리해 검증한다.

### 2026-08-01 — 키보드 디버그 스테이지 선택

- [x] URL query 없이 백틱(`) 키로 디버그 모드를 토글하도록 `Game` 입력 흐름에 추가했다. 토글 키 자체가 타이틀의 일반 입력으로 소비되지 않도록 해당 프레임을 조기 종료한다.
- [x] 디버그 모드에서 숫자 1–9와 0으로 stage 1–10을 즉시 시작하고, `Shift+1`–`Shift+5`로 stage 11–15를 선택하도록 구현했다. 숫자패드 입력도 같은 매핑을 지원한다.
- [x] 디버그 스테이지 진입 시 score/record/lives/회차/클리어 기록을 초기화하고, 입구 연출을 건너뛴 활성 플레이 상태와 원본 스테이지 음악으로 시작한다.
- [x] `tools/check_kingsvalley_gameplay.mjs`에 백틱 토글, stage 5 직접 시작, Shift stage 11/15 매핑 회귀를 추가했다. 기존 15개 스테이지 전이·엔딩 회귀도 통과했다.
- [x] `web/README.md`에 키 조작과 자동 캡처용 legacy URL의 용도를 분리해 기록했다. URL은 일반 디버그 진입에 필요하지 않다.

#### 키보드 디버그 후 남은 작업

- [ ] 실제 브라우저에서 백틱 토글과 숫자키 포커스/키보드 레이아웃을 최종 확인한다. Node 회귀에서는 입력 edge와 스테이지 상태를 검증했다.
- [ ] 필요 시 디버그 선택 시 입구 연출 유지 여부와 score/lives 초기화 정책을 원본 디버거용 preset으로 세분화한다.

### 2026-08-01 — 2회차 미라 난이도 연결

- [x] 원본 `initMomia`의 `numFinishGame + tipo` 계산을 `Level` 생성 옵션으로 반영했다. 완료 회차만큼 미라 타입을 올리고 타입 4에서 포화한다.
- [x] 타입 상승에 따라 원본 `tiposMomia`의 속도·색상 테이블이 다음 스테이지 생성에 적용되도록 `Game.startLevel()`과 재시작 경로에 회차를 전달했다.
- [x] 1회차·2회차·5회차(타입 4 포화) 미라 생성 회귀와 기존 15개 스테이지·엔딩 통합 회귀를 통과했다.
- [x] 실제 브라우저가 없는 환경을 보완하도록 `Input.attach()`의 백틱·Shift+숫자 keydown/keyup edge 회귀를 추가했다.

#### 2회차 작업 후 남은 작업

- [ ] 2회차 실제 장기 플레이의 미라 AI framebuffer exact를 openMSX reference에 추가한다. 타입·속도·색상 초기화는 완료했지만 장기 SAT 비교는 1회차 기준만 있다.

### 2026-08-01 — 아이템 획득 순서·충돌 범위 보정

- [x] 원본 `tickGame` 순서(`AI_Cuchillos` 뒤 `chkCogeKnife`)에 맞춰 JS의 칼·곡괭이 획득 판정을 엔티티 갱신 뒤로 이동했다. 칼이 적중·낙하하는 프레임과 플레이어 획득 프레임이 같은 순서를 사용한다.
- [x] 원본 `itemHitArea`의 우측 경계에서 플레이어가 칼을 획득하지 못하던 1픽셀 차이를 보정하고, stage 1 아이템 회귀 fixture를 경계 좌표로 강화했다.
- [x] 전체 데모에서 frame 160의 배경 오차 18픽셀을 제거했고, 대표 framebuffer 잔여 합계는 2273→2255픽셀로 감소했다. 고정소수점 시작값 변경 실험은 2980픽셀로 악화되어 적용하지 않았다.

#### 아이템 순서 보정 후 남은 작업

- [ ] 주인공의 `ProtaXdecimal`·`protaMovCnt`가 비교 시점에 2틱 앞서는 장기 phase를 입구 생략/데모 캡처 경계와 분리해 보정한다.
- [ ] frame 160 이후 플레이어 칼 sprite의 패턴·방향 표시 시점을 원본 `setAttribProta`와 대조한다.

### 2026-08-01 — 주인공 frameProta·브라우저 런타임 점검

- [x] 원본 `calcFrame`과 `framesProta`의 8단계 애니메이션 표를 JS 렌더링에 직접 반영했다. 기존 임의 32단계 offset을 제거해 대표 장기 데모 framebuffer 잔여 합계를 2255→1841픽셀로 줄였다.
- [x] 탭 전환 시 방향키·액션 edge를 초기화하고, AudioContext가 suspend된 뒤 visibility 복귀 시 재개하도록 `Input`·`Sound`를 보강했다.
- [x] `tools/check_kingsvalley_browser.mjs`에 256×192 raster, 59.92Hz, resize, Space-only 입력, blur/visibility, AudioContext 계약 검사를 추가했다. 실제 브라우저 바이너리가 없는 환경에서도 런타임 계약을 자동 검증한다.
- [x] `tools/check_kingsvalley_gameplay.mjs`가 15개 스테이지 각각에 방향·계단·점프·액션 입력을 96틱 주입하고 좌표 유효성을 확인한다. 기존 보석·출구 전이 회귀와 함께 실행된다.
- [x] 전체 비교 결과를 `build/comparison-player-frame-exact.json`에 저장했다. 고정소수점·계단·낙하의 일부 장기 차이는 실제 상태 phase 문제로 남겨 별도 항목으로 유지한다.

#### 주인공 frameProta 후 남은 작업

- [ ] frame 0008·0162·1472처럼 입력 phase 또는 낙하 시작이 다른 구간을 원본 `AI_Prota`·`chkProtaCae` 호출 시점과 재대조한다.
- [ ] 실제 브라우저에서 AudioContext 권한, 캔버스 크기, 백틱·숫자키 포커스 동작을 수동 확인한다. 현재는 브라우저 계약 자동 검사까지 완료했다.

### 2026-08-01 — 낙하·미라 계단 sprite phase 보정

- [x] 원본 `chkProtaCae` 호출 시점과 비교해 지상 이탈을 한 프레임 예약했다. frame 1472 낙하 sprite가 원본과 일치하고 대표 장기 framebuffer 잔여가 1841→1239픽셀로 줄었다.
- [x] 원본 sprite 갱신 전 미라 상태를 보존하고, type-3 미라가 하단 플랫폼 계단에서 이동하는 경우에만 X-1/Y+1 표시 보정을 적용했다. frame 176·192 framebuffer가 52→0픽셀이 됐다.
- [x] 보정 조건을 현재 미라 상태·이전 상태·플레이어 Y까지 제한해 다른 방과 계단의 정상 슬롯을 보호했다. 최종 결과는 `build/comparison-residual-final.json`에 기록했다.

#### 낙하·미라 보정 후 남은 작업

- [ ] 초기 데모 frame 0008–0064의 지상 sprite Y/이동 phase 잔여를 입구 연출 타이밍과 분리한다.
- [ ] frame 1216·1344·1600·1984의 주인공 장기 X/Y phase를 원본 `chkPisaSuelo`·`andaEscalera` 호출 순서와 추가 대조한다.

### 2026-08-01 — 주인공 초기 이동 phase 추가 보정

- [x] 원본 시작 상태의 `protaMovCnt=1`을 `Player` 생성·리셋 상태에 반영했다. 기록 데모 framebuffer 잔여 합계가 1135→646픽셀으로 감소했다.
- [x] 낙하 시작을 한 프레임 예약한 변경에 맞춰 자산 회귀 fixture를 2회 update 기준으로 갱신했다.
- [x] `check_kingsvalley_assets`, `check_kingsvalley_gameplay`, `check_kingsvalley_browser`, JavaScript syntax check, `git diff --check`를 통과했다.
- [x] 최신 비교 결과를 `build/comparison-residual-final.json`에 저장했다. 배경 VRAM 잔여는 0픽셀이다.

#### 초기 이동 phase 보정 후 남은 작업

- [ ] 계단 진입 직후 frame 0016·0032·0048·0064의 주인공 X/Y 1픽셀 phase를 원본 `andaEscalera`의 홀짝 timer와 분리해 보정한다(합계 276픽셀).
- [ ] frame 0456·1344·1600·1728·1984에 남은 sprite 합계 370픽셀을 고정소수점·계단 전환·VBlank 반영 순서로 대조한다.
- [ ] 15개 스테이지의 실제 보석 수집·출구 진입 장기 입력 녹화를 추가한다. 현재는 각 스테이지 96틱 입력 smoke 및 강제 클리어 회귀까지 완료했다.
- [ ] 실행 환경에 브라우저 바이너리가 설치되면 실제 AudioContext·창 비율·키 포커스 수동 점검을 수행한다. 현재 자동 런타임 계약 7/7 통과 상태다.

### 2026-08-01 — 2스테이지 하강 계단 바닥 경계 수정

- [x] 원본 `chkFinEscalera`가 마지막 맵 행 아래의 경계 타일을 플랫폼으로 판정하는 동작을 확인했다.
- [x] JS `isStairExit`가 `mapIds` 배열 밖을 빈 타일로 보던 문제를 수정해, 하단 경계에서도 계단을 종료하고 바닥에 정지하도록 했다.
- [x] 2스테이지 하강 계단 회귀를 `tools/check_kingsvalley_assets.mjs`에 추가했다. 재현 경로는 Y=160에서 `onLadder=false`, `onGround=true`가 된다.

### 남은 작업

- [ ] **주인공 동적 SAT 정밀도**: 고정소수점 X, 보행 frame, 계단 진입·이탈 시점과 VBlank 직전 sprite 반영 순서를 원본과 일치시킨다.
- [ ] **미라 동적 SAT 정밀도**: 벽 반전 예약 시점, 계단 경로, 림보·재등장 animation frame의 표시 시점을 원본과 일치시킨다.
- [ ] **장기 데모 framebuffer exact**: 최신 전체 비교에서 잔여 프레임은 `0016/0032/0048/0064/0456/1344/1600/1728/1984`의 `66/75/75/60/63/91/68/65/83`픽셀이며, 전체 데모 framebuffer 잔여 합계는 646픽셀, 배경 VRAM 잔여는 0픽셀이다. 결과는 `build/comparison-residual-final.json`에 기록했다.
- [ ] **실제 조작 경로 검증 확대**: 전 스테이지 입력 smoke와 상태 강제 전이는 완료했지만, 각 스테이지에서 실제 이동으로 모든 보석을 먹고 출구를 여는 장기 입력 녹화는 추가로 필요하다.
- [ ] **브라우저 수동 최종 점검**: 실제 브라우저 AudioContext 시작, 창 크기별 화면 비율, 키 입력 포커스·blur 상황을 브라우저에서 최종 확인한다.

위 항목은 기능 포팅 완료 상태를 되돌리지 않고, `kvalley.lst`와 openMSX의 프레임·입력 타이밍을 맞추는 후속 작업이다.
